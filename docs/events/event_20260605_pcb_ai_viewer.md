# Event: PCB AI Viewer Planning

## 需求

建立 `bodesign` host-agnostic MCP server + web server：agent/IDE 透過 MCP 提供檔案或設計指令；網站註冊在 `/bodesign/`，顯示 Rockbox 電路/PCB view。畫面可來自 Gerber/IPC 逆向重建，也可來自 datasheet/component knowledge 經 AI 生成的 `BoardDesign IR`。MCP server 本身必須能吸收 datasheet/reference documents 成為知識庫，擴大各種晶片與元件資料，以便被要求時能拿出來畫電路。

## 範圍 IN

- Host-agnostic MCP server；opencms、Cursor、Claude Desktop、VS Code 等 IDE/agent client 都可串接。
- `/bodesign/` web server/viewer。
- FastAPI/Python EDA service。
- OpenMV schematic/datasheet/reference document ingestion into design evidence。
- Rockbox Gerber/artwork、drill、IPC netlist、routing/report artifact upload and detection。
- `BoardDesign IR` generation/reconstruction with confidence scores and evidence refs。
- Datasheet/component knowledge normalization for PDFs and heterogeneous documents as Day 1 core capability。
- `/bodesign/` rendering for Rockbox reconstructed circuit/PCB view, original Gerber, generated Gerber, component knowledge, evidence, and confidence overlays。
- MCP knowledge ingestion for datasheets/reference documents so component/chip knowledge can be reused in later design requests。
- Gerber generation/export from `BoardDesign IR` with deterministic validation and user approval gate。

## 範圍 OUT

- 第一版不支援 silent automatic modification。
- 第一版不保證完整取代專業 EDA/DFM 工具。
- 第一版不保證復原原始 schematic、原始 EDA project、原始 constraints 或完整原始設計意圖。
- 出廠測試/debug 指引、任兩點理論電壓/電阻計算延後到後續研究，不納入本階段 MVP。
- 第一版不處理多人協作、付款、組織權限。

## 任務清單

- T1 Define source-ingestion taxonomy and evidence schemas。
- T2 Define ComponentKnowledge, DesignIntent, and BoardDesign IR schemas。
- T3 Scaffold MCP server, FastAPI service, and `/bodesign/` web viewer。
- T4 Implement upload, artifact/document detection, and job model。
- T5 Add OpenMV document-to-knowledge/design planning interfaces。
- T6 Add Rockbox component, Gerber, drill, and IPC-to-IR interfaces。
- T7 Render Rockbox BoardDesign IR and Gerber layers at `/bodesign/`。
- T8 Add Gerber generation and validation from BoardDesign IR。
- T9 Add MCP AI planning tools for OpenMV and Rockbox flows。
- T10 Produce design/reconstruction reports for generated outputs。
- T11 Add datasheet knowledge ingestion and reuse tests。
- T12 Add targeted backend, MCP, and frontend verification。

## Key Decisions

- MVP scope selected: complete viewing → AI proposal → approval → export loop。
- Stack selected: React frontend + FastAPI backend。
- AI mode selected: assisted modification with explicit user approval。
- Persistence selected: Postgres。
- Renderer selected: Canvas 2D first。
- Parser strategy selected: Python library first。
- User requested GitHub open-source background research before scaffold。
- Product direction changed again: user likely has only Gerber/manufacturing outputs, so AI should reverse-engineer an editable source-layout model from Gerber/drill/IPC evidence。
- Editing strategy changed: AI proposals should patch reconstructed source objects, then regenerate Gerber outputs and report confidence/deltas。
- Scope narrowed: debug/test guidance is deferred; MVP focuses only on OpenMV docs → layout/Gerber, Rockbox Gerber → layout, and browser layout rendering。
- Source of truth changed: `BoardDesign IR` becomes the product-owned source for both generated and reconstructed layouts。
- EDA kernel decision: `pygerber` is retained for Gerber ingestion/validation only; core PCB layout design must be based on product-owned `BoardDesign IR` plus EDA bridge adapters。
- EDA bridge direction: KiCad is the first practical bridge candidate; `freerouting` is the first autorouting candidate after IR/constraints exist; `skidl` may help convert OpenMV docs into circuit/netlist evidence。
- Component knowledge decision: datasheet ingestion and normalized component records must be Day 1 architecture, because AI layout analysis requires pinouts, package, power/interface roles, and layout guidelines。
- Responsibility decision: source ingestion and normalization is the first responsibility area; display, analysis, and design authoring depend on normalized core data rather than raw PDFs/Gerbers directly。
- Host decision: opencms/opencode is the base infrastructure for dialog, file explorer, agent harness, approval flow, and fileview; EDA/Gerber should be delivered as MCP/plugin capability instead of a standalone app shell。
- Gateway decision: richer EDA web surfaces can register directly on the opencms gateway, keeping viewer deployment inside the host instead of launching an unrelated standalone web app。
- Product boundary decision: bodesign should now be host-agnostic MCP + web server; opencms is an optional first-class integration, not the required shell。
- MVP vision decision: first visible milestone is `/bodesign/` showing Rockbox circuit/PCB view after agent-provided files or instructions, backed by reconstruction or datasheet-derived generation。
- Knowledge-base decision: MCP server must include knowledge ingestion so datasheets/reference documents can be absorbed and reused for future chip/component design requests。

## GitHub Research Summary

- `KiCad/kicad-source-mirror`: active GPL-3.0 EDA reference and bridge candidate for file/export/DRC semantics。
- `freerouting/freerouting`: active GPL-3.0 DSN/SPECCTRA autorouter candidate for deterministic routing experiments。
- `skidl`: MIT Python code-first circuit/netlist tool; useful for OpenMV docs → structured circuit evidence, not layout engine。
- `kinparse`: MIT KiCad V5-V9 netlist parser; useful if KiCad netlist becomes interchange evidence。
- `Argmaster/pygerber`: recommended primary backend candidate; MIT, Python, PyPI package, supports Gerber X3/X2/RS-274X/RS-274D, API + CLI rendering。
- `tracespace/tracespace`: strong TypeScript web visualization reference with parser/plotter/renderer packages, but maintainer states the project is on indefinite hiatus; avoid hard dependency initially。
- `gerbv/gerbv`: mature maintained Gerber viewer/reference tool, but GPL/native dependency makes product embedding risky without license review。
- `curtacircuitos/pcb-tools`: archived Python project; use only as historical reference。
- `xingrz/GerberViewer`: Apache-2.0 web viewer reference, Vue-based; useful for UX comparison, not ideal as React core。
- Reverse reconstruction research is now required; `.art`, `.drl`, `.ipc`, and `.rou` packages should be treated as primary inputs。

## Debug Checkpoints

- Planning checkpoint: repository had no existing source, specs, or docs at task start。
- Safety checkpoint: no fallback or automatic design modification is allowed in the proposed workflow。
- Research checkpoint: `gh` and `curl` were available; GitHub searches found viable parser/viewer candidates, but generic `pcb drc gerber` searches returned no strong dedicated DRC candidate。
- License checkpoint: GPL tools should remain reference/comparison tools until license review is complete。
- Reverse-source checkpoint: Gerber is not original source, but Gerber plus drill plus IPC netlist can be used to infer a confidence-scored editable layout model。
- Local data checkpoint: `01.ROCKBOX` contains a high-value Allegro 22.1 manufacturing package with RS-274X `.art`, `.drl`, IPC-D-356A `.ipc`, `.rou`, stackup spreadsheet, and panel PDF。
- IPC evidence checkpoint: Rockbox IPC-D-356A includes layer metadata, padstack data, named nets, refdes, pin numbers, coordinates, pad sizes, side markers, and via records; this is sufficient to design an initial reconstruction spike。
- Component evidence checkpoint: Rockbox `cds2f_ROCKBOX_V2.txt` exposes refdes, part number/value, package symbol, side, rotation, and XY placement; examples include `MDBT53-P1M`, `AN7002Q`, `W25Q128JVSIQ`, `RT9471DGQW`, and `TCK106AG_LF`。
- OpenMV checkpoint: `02.OpenMV` currently contains schematic PDF and datasheets for target subsystems, but no Gerber/source package was found in the scanned folder。
- EDA kernel checkpoint: current environment does not have `kicad-cli`/`pcbnew`, so scaffold must abstract EDA bridge interfaces and not require KiCad on day one。
- Scaffold checkpoint: initial host-agnostic MCP/server/web skeleton created with `apps/web`, `services/api`, `services/mcp`, core package placeholders, optional `integrations/opencms`, root README, FastAPI placeholder endpoints, and MCP planned tool manifest。
- Contract checkpoint: added minimal Python dataclass contracts for `EvidenceSource`, `EvidenceRef`, `InputArtifact`, `ProjectSummary`, `ComponentKnowledge`, and `BoardDesign`; added a Rockbox reconstruction placeholder that returns an empty `BoardDesign` with evidence/confidence placeholders and performs no real parsing。
- T4 checkpoint: added deterministic artifact type detection for datasheet/reference PDFs, schematic names, BOM/placement files, Gerber/artwork, drill, IPC-356, routing reports, and unknown files; added in-memory project/job placeholder APIs without persistence or real file storage。
- T5 checkpoint: added OpenMV document-to-knowledge/design planning placeholders with `DesignIntent`, explicit knowledge gaps, component hints from document names, and `POST /api/projects/{project_id}/openmv/plan`; no PDF parsing or external lookup is implemented。
- T6 checkpoint: added Rockbox input manifest placeholder for component placement/BOM files, Gerber/artwork, drill, IPC-356, routing reports, and unknown files; added manifest and reconstruction API endpoints that return artifact-count confidence summaries without real geometry parsing。
- T7 checkpoint: upgraded `/bodesign/` from text placeholder to a visual Rockbox viewer mock showing a board canvas, representative components/traces, six-layer labels, BoardDesign IR identity, artifact-count evidence, and placeholder confidence status。
- T8 checkpoint: added Gerber export and validation placeholders with source-core export plans, gerber-core validation warnings, and API endpoints for `POST /api/projects/{project_id}/export/gerber` and `POST /api/projects/{project_id}/export/gerber/validate`; no real Gerber files are generated or inspected。
- T9 checkpoint: added `services/mcp/bodesign_mcp.py` placeholder handlers for `ingest_sources`, `ingest_knowledge`, `normalize_sources`, `reconstruct_board`, `generate_board`, `validate_design`, `export_gerber`, and `open_viewer`; handlers call existing deterministic placeholder contracts and do not provide full MCP transport yet。
- T10 checkpoint: exposed `DesignReport` through `POST /api/projects/{project_id}/reports/design`, MCP `produce_report`, and the MCP tool manifest; reports summarize reconstruction/export assumptions, artifact references, warnings, and send-to-fab caveats。
- T11 checkpoint: added component-kb datasheet ingestion and reuse placeholders with reusable component keys, source evidence refs, explicit extraction knowledge gaps, `POST /api/projects/{project_id}/knowledge/datasheets`, MCP `reuse_knowledge`, and unit coverage for ingestion/reuse。
- T12 checkpoint: completed targeted backend/MCP verification for artifact ingestion, component knowledge ingestion/reuse, Rockbox reconstruction placeholder, Gerber export placeholder, report generation, and `/bodesign/` viewer URL generation。
- Route checkpoint: registered bodesign-facing web/API aliases under `/bodesign/`, `/bodesign/health`, and `/bodesign/api/*` while keeping legacy `/api/*` aliases for scaffold compatibility。
- Rockbox summary checkpoint: upgraded reverse-core from artifact-count placeholder to fixture-backed summary reconstruction. It parses Allegro `cds2f_ROCKBOX_V2.txt` into 327 component instances, IPC-D-356A into 208 named nets, 938 component pads, and 817 vias, and preserves six copper layer labels from the Gerber manifest。
- Viewer checkpoint: `/bodesign/` now renders fixture-derived component/net/layer counts, selected placed components such as `U401 MDBT53-P1M`, and top IPC net summaries instead of only static mock data. Exact Gerber geometry remains pending。
- Visible route checkpoint: added `/` redirect to `/bodesign/`, HTML route index at `/bodesign/routes`, JSON route registry at `/bodesign/api/routes`, and a `python3 -m services.api` uvicorn entrypoint so the web surface has an explicit launch path。
- Published Web checkpoint: fixed opencms visibility by adding a `bodesign` entry to `~/.config/web_registry.json`, publishing gateway route `/bodesign -> 127.0.0.1:8765`, adding repo `webctl.sh`, and starting the FastAPI upstream on port 8765. Root cause: app routes existed inside FastAPI, but opencms Published Web only lists gateway-published routes with matching user registry entries。
- Workspace viewer checkpoint: replaced the single-board mock with a multi-tab file workspace covering Source Documents, Board View, Gerber Layers, IPC/Nets, Components, BoardDesign IR, and Reconstruction Report. This explicitly marks the board drawing as placement-summary only and moves correctness-sensitive evidence into file/type-specific views。
- Project browser checkpoint: added a web Projects tab and `GET /bodesign/api/projects` builtin project registry. Rockbox is now listed as an already-imported fixture project with artifact/component/net counts and an Open/Browse entry instead of being only an implicit demo page。
- Board View correction checkpoint: removed the decorative placement-sketch canvas from Board View because it was visually misleading and not a valid schematic, circuit diagram, or Gerber render. Board View now shows an explicit “PCB layout rendering is not available yet” state and points users to Gerber Layers, IPC/Nets, and Components for verified evidence。
- Roadmap checkpoint: expanded the plan from T1–T12 scaffold work into T13–T27 functional gap tasks. The next capability sequence is real project workspace, source-file viewers, Gerber/drill geometry reconstruction, component-pad-net fusion, component knowledge extraction, KiCad bridge evaluation, AI reference-board workflow, and generated design candidate approval UI。
- T13-T15 checkpoint: added per-project workspace route `/bodesign/projects/{project_id}`, artifact browser route `/bodesign/projects/{project_id}/artifacts/{artifact_id}`, project artifact APIs, fixture-backed Rockbox artifact records, and basic type-aware artifact preview pages. T13/T14 remain partial because durable DB/file storage and upload/drop import are still pending; T15 basic file viewers are complete for current fixture evidence.
- T16-T19 checkpoint: added a lightweight gerber-core geometry parser for Rockbox-style RS-274X `.art` files and Excellon `.drl` files. `/bodesign/` Board View now renders evidence-based SVG from `L1_top.art` draw/flash operations plus `ROCKBOX_V2-1-6.drl` drill hits, and artifact pages show Gerber/Drill geometry summaries instead of raw text only. This is a parser/render spike, not full EDA editing.
- Viewer backend checkpoint: evaluated pygerber as the first third-party Gerber renderer. Raw Allegro `.art` headers are not accepted directly, but gerber-core now normalizes Allegro compatibility blocks (`IR/IP/OFA/MI/SF`) and renders Rockbox `L1_top.art` through pygerber SVG when the dependency is installed. The internal geometry SVG remains a fallback only.
- Board View viewport checkpoint: changed Board View from page-expanding CSS zoom to a fixed-height viewport with SVG viewBox zoom/pan controls. The pygerber SVG is now focused to percentile-bounded visible Gerber flash positions so sparse corner/tooling marks no longer dominate the default fit.
- Board View mouse interaction checkpoint: added mouse-wheel zoom at cursor and drag-to-pan behavior inside the fixed Board View viewport. Interaction updates the SVG viewBox rather than expanding the page scroll area.
- Board View component overlay checkpoint: added a placement-derived component overlay on top of the pygerber preview. Default markers focus on major ICs/connectors/antenna/crystals/switches, optional toggles reveal passives/test points, and clicking a marker opens an inspector with placement, footprint, side, XY, and IPC-derived pin/net evidence. This is not yet exact footprint geometry; it is an evidence overlay from Allegro placement and IPC-356 data.
- Board View copper flash checkpoint: identified the confusing green dots as pygerber-rendered Gerber flash apertures from the selected copper layer, not synthetic component markers. Board View now hides SVG `<use>` flash apertures by default and provides a `Toggle copper pads/flashes` control so users can inspect them intentionally without confusing the default single-layer preview for a shorted net view.
- Board View overlay coordinate checkpoint: disabled placement overlay by default because Allegro placement coordinates are not yet calibrated to exact pygerber/Gerber footprint geometry. When explicitly toggled on, markers are now reprojected point-by-point from the current SVG viewBox during pan/zoom instead of moving as a separately scaled HTML layer.
- Workspace layout checkpoint: fixed full-width tab layout by forcing the tab panel container to occupy a full flex row, constraining the app shell to `minmax(0, 1fr)`, and adding overflow guards for panels, grids, cards, tables, code, and JSON previews.
- Plan checkpoint: promoted Board View stabilization into the active plan before T20. The next required slice is pygerber raster-only rendering with no hand-written SVG fallback, explicit render-failure UI/API states, synchronized tests/docs, and a restored green validation baseline before component-pad-net reconstruction starts.

## Verification

- Planning artifacts created under `plans/product_pcb_ai_viewer/`。
- Architecture baseline created at `specs/architecture.md`。
- Technology research updated at `plans/product_pcb_ai_viewer/technology-research.md`。
- Architecture Sync: Updated from source-first assumption to Gerber/drill/IPC reverse-to-source copilot with confidence-scored reconstructed model。
- Scaffold validation: FastAPI app exposes `GET /health`, `GET /bodesign/`, and `GET /api/projects`; syntax/import validation attempted without installing dependencies。
- Contract validation: FastAPI app now exposes `GET /api/schema-summary` and `GET /api/projects/{project_id}/board-design`; Python compile validation covers the touched service and contract modules。
- T4 validation: Python compile validation covers API and shared detection modules; deterministic detection smoke test covers Rockbox-style `.art`, `.drl`, `.ipc`, `.rou`, `cds2f` placement, schematic PDF, datasheet PDF, and unknown files。
- T5 validation: Python compile validation covers API and doc-core modules; import smoke test covers `plan_openmv_document_ingestion` returning a placeholder `DesignIntent` with knowledge gaps。
- T6 validation: Python compile validation covers API and reverse-core modules; Rockbox smoke test covers `.art`, `.drl`, `.ipc`, `.rou`, and `cds2f` manifest classification plus placeholder `BoardDesign` confidence counts。
- T7 validation: Python compile validation covers `/bodesign/` viewer route; viewer remains manifest-backed and intentionally does not parse real Gerber geometry yet。
- T8 validation: Python compile validation covers API, source-core, and gerber-core modules; smoke test covers placeholder export plan output paths and placeholder Gerber validation warnings。
- T9/T10 validation: Python compile validation covers API, MCP handler module, and source-core report contracts; MCP smoke test covers `produce_report`, `export_gerber`, and `validate_design` placeholder outputs。
- T11 validation: Python compile validation covers component-kb, API, MCP handlers, and component tests; `unittest` covers datasheet ingestion creating `component:mdbt53-p1m` and reusing `W25Q128JVSIQ` knowledge; MCP smoke test covers `ingest_knowledge` and `reuse_knowledge`。
- T12 validation: `python3 -m compileall packages services tests`, `python3 -m unittest discover -s tests`, and MCP end-to-end smoke for ingest → knowledge → reconstruct → export → report → viewer all passed。
- Route validation: `python3 -m compileall services/api/main.py tests packages services` and `python3 -m unittest discover -s tests` passed; route-table smoke uses a FastAPI stub to verify `/bodesign`, `/bodesign/`, `/bodesign/health`, and `/bodesign/api/*` aliases without requiring FastAPI to be installed locally。
- Rockbox summary validation: `python3 -m compileall packages services tests`, `PYTHONPATH="packages/shared:packages/design-ir:packages/reverse-core:packages/component-kb:packages/doc-core:packages/source-core:packages/gerber-core" python3 -m unittest discover -s tests`, and MCP `reconstruct_board`/`open_viewer` smoke test passed. The new reverse-core fixture test fixes expected Rockbox counts at 327 components, 208 nets, 6 layers, 938 IPC pads, and 817 vias。
- Viewer validation: FastAPI is not installed locally, so `/bodesign/` helper validation used the existing FastAPI stub approach and confirmed the HTML contains `MDBT53-P1M`, `Top Nets`, and `327` from fixture-derived data。
- Architecture Sync: Updated `specs/architecture.md` to distinguish current placement/IPC summary reconstruction from pending Gerber/drill geometry extraction。
- Visible route validation: `python3 -m compileall services/api/main.py services/api/__main__.py tests packages services` and `PYTHONPATH="packages/shared:packages/design-ir:packages/reverse-core:packages/component-kb:packages/doc-core:packages/source-core:packages/gerber-core" python3 -m unittest discover -s tests` passed; tests now assert `/`, `/bodesign/routes`, and `/bodesign/api/routes` registration and route-index content without requiring FastAPI to be installed locally。
- Published Web validation: local upstream `http://127.0.0.1:8765/bodesign/` returned `200 text/html`; gateway route `http://127.0.0.1:1080/bodesign/` returned `200 text/html`; opencode `webctl.sh list-routes` shows `/bodesign 127.0.0.1:8765 uid=1000`; `python3 -m json.tool /home/pkcs12/.config/web_registry.json` validates the registry after adding the entry。
- Workspace viewer validation: `python3 -m compileall services/api/main.py tests packages services` and `PYTHONPATH="packages/shared:packages/design-ir:packages/reverse-core:packages/component-kb:packages/doc-core:packages/source-core:packages/gerber-core:." python3 -m unittest discover -s tests` passed; route tests now assert the viewer contains the file workspace tabs and warns that the board view is not yet a true schematic/Gerber render。
- Project browser validation: `python3 -m compileall services/api/main.py tests packages services` and `PYTHONPATH="packages/shared:packages/design-ir:packages/reverse-core:packages/component-kb:packages/doc-core:packages/source-core:packages/gerber-core:." python3 -m unittest discover -s tests` passed; route tests now assert Rockbox appears in both the web Projects tab and project API as `imported-fixture`。
- Board View correction validation: `python3 -m compileall services/api/main.py tests packages services`, `PYTHONPATH="packages/shared:packages/design-ir:packages/reverse-core:packages/component-kb:packages/doc-core:packages/source-core:packages/gerber-core:." python3 -m unittest discover -s tests`, and gateway checks for `PCB layout rendering is not available yet` / `decorative placement sketch` passed after restarting bodesign。
- Roadmap validation: planning-only update; synchronized `implementation-spec.md`, `tasks.md`, `specs/architecture.md`, and this event log. Architecture Sync: updated immediate capability gaps and preserved Board View no-fake-rendering rule。
- T13-T15 validation: `python3 -m compileall services/api/main.py tests packages services` and `PYTHONPATH="packages/shared:packages/design-ir:packages/reverse-core:packages/component-kb:packages/doc-core:packages/source-core:packages/gerber-core:." python3 -m unittest discover -s tests` passed; route tests now assert project workspace routes, artifact APIs, and Rockbox artifact preview pages.
- T16-T19 validation: `python3 -m compileall -f packages services tests` and `PYTHONPATH="packages/component-kb:packages/design-ir:packages/doc-core:packages/gerber-core:packages/reverse-core:packages/shared:packages/source-core:services/mcp" python3 -B -m unittest discover -s tests` passed; parser tests assert Rockbox `L1_top.art` produces >1000 draw segments, >800 flashes, 40+ apertures, and `ROCKBOX_V2-1-6.drl` exposes 789 explicit drill hits plus four tool records with plated/non-plated hints.
- Viewer backend validation: `python3 -m compileall -f packages services tests` and system-Python unittest passed with pygerber render skipped when unavailable; `.venv/bin/python -m unittest tests.test_gerber_geometry.GerberGeometryTests.test_pygerber_adapter_renders_normalized_rockbox_layer_when_available` rendered the normalized Rockbox top layer to SVG successfully.
- Board View overlay validation: `python3 -m compileall -f services/api/main.py tests` and `PYTHONPATH="packages/shared:packages/design-ir:packages/component-kb:packages/doc-core:packages/reverse-core:packages/gerber-core:packages/source-core:services/api:services/mcp" python3 -m unittest discover -s tests` passed; local bodesign upstream `http://127.0.0.1:8765/bodesign/projects/rockbox` returned component overlay markers including `U401 MDBT53-P1M`, category toggles, and IPC pin/net inspector markup.
- Board View copper flash validation: `python3 -m compileall -f services/api/main.py tests` and full unittest discovery passed; local bodesign upstream `http://127.0.0.1:8765/bodesign/projects/rockbox` returned CSS that hides `svg use` flash apertures unless `.show-copper-flashes` is enabled and includes the `Toggle copper pads/flashes` control.
- Board View overlay coordinate validation: `python3 -m compileall -f services/api/main.py tests` and full unittest discovery passed; local bodesign upstream returned `Toggle placement overlay`, the default-off overlay warning, and point-by-point SVG viewBox projection code without the previous `overlay.style.transform` layer transform.
- Workspace layout validation: `python3 -m compileall -f services/api/main.py tests` and full unittest discovery passed; local bodesign upstream returned `.panels { flex: 0 0 100% }`, `grid-template-columns: 300px minmax(0, 1fr)`, and `table-layout: fixed` CSS for full-width workspace stability.
- Plan update validation: synchronized `tasks.md`, `implementation-spec.md`, `specs/architecture.md`, and this event log so raster-only Board View stabilization is the gate before T20.
- Raster-only stabilization validation: `python3 -m compileall -f packages/gerber-core services/api/main.py tests` and `PYTHONPATH="packages/shared:packages/design-ir:packages/component-matcher:packages/reverse-core:packages/gerber-core:services/api" python3 -m unittest tests.test_api_routes tests.test_gerber_geometry` passed after aligning Board View copy/tests to the `pygerber-raster` + fail-fast contract.
- T20 first-pass fusion validation: `PYTHONPATH="packages/shared:packages/design-ir:packages/component-knowledge:packages/source-core:packages/reverse-core:packages/gerber-core:services/api" python3 -m unittest tests.test_api_routes tests.test_gerber_geometry` passed; geometry API now exposes `fusion_summary` with Rockbox component/net coverage and Board View shows a component-net fusion preview from placement + IPC evidence. Architecture Sync: first-pass fusion is documented as non-spatial; Gerber pad/flash/via/drill spatial fusion remains pending.
- T21 first-pass cross-probing validation: `python3 -m compileall -f services/api/main.py tests/test_api_routes.py` and `PYTHONPATH="packages/shared:packages/design-ir:packages/component-knowledge:packages/source-core:packages/reverse-core:packages/gerber-core:services/api" python3 -m unittest tests.test_api_routes` passed. Added `/bodesign/api/projects/{project_id}/cross-probe/{probe_id}` for component, net, and artifact lookups, plus Board View table links for component/net/artifact probes. Architecture Sync: cross-probing is currently evidence-index based; layer/object-level derived IR probing waits for spatial geometry primitives.
- T22 Rockbox knowledge queue validation: `python3 -m compileall -f packages/component-kb services/api/main.py tests` and `PYTHONPATH="packages/shared:packages/design-ir:packages/component-knowledge:packages/component-kb:packages/source-core:packages/reverse-core:packages/gerber-core:services/api" python3 -m unittest tests.test_component_kb tests.test_api_routes` passed. Added component-kb queue contracts and `/bodesign/api/projects/{project_id}/knowledge/queue` for reusable Rockbox part candidates with priorities and knowledge gaps. Architecture Sync: OpenMV reference extraction is still pending.
- T23 user-provided datasheet extraction validation: `python3 -m compileall -f packages/component-kb tests/test_component_kb.py` and `PYTHONPATH="packages/shared:packages/component-kb" python3 -m unittest tests.test_component_kb` passed. `ingest_datasheet_knowledge` now reads local text-like datasheets, performs best-effort PDF token scanning without external downloads, records extracted text length/fields, infers package hints, and keeps explicit gaps for structured pin/electrical extraction.
- T23b planning checkpoint: user suggested combining a docxmcp workflow for PDF document understanding. Decision: adopt it as a better architecture direction because PDF pages/tables/text should become chunked source assets with provenance before component extraction; this avoids treating a PDF blob as a single lossy string and gives later pinout/layout extraction stable evidence anchors.
- T23b adapter validation: `python3 -m compileall -f packages/doc-core tests/test_doc_core.py` and `PYTHONPATH="packages/shared:packages/doc-core" python3 -m unittest tests.test_doc_core` passed. Added `DocumentSourceChunk` and `document_to_source_chunks` in doc-core so local text/PDF-like documents can become chunked source assets with evidence anchors before downstream component extraction.
- Cross-MCP product strategy checkpoint: fastest MVP should ask the client/agent to orchestrate bodesign MCP and docxmcp as two services, passing docxmcp PDF-derived source chunks into bodesign ingestion. Internal MCP-to-MCP direct connection is possible later, but should sit behind an adapter boundary and not hard-couple bodesign server runtime to docxmcp accounts, permissions, or availability.
- T24 external fetch policy validation: `python3 -m compileall -f services/api/main.py tests/test_api_routes.py` and `PYTHONPATH="packages/shared:packages/design-ir:packages/component-knowledge:packages/component-kb:packages/doc-core:packages/source-core:packages/reverse-core:packages/gerber-core:services/api" python3 -m unittest tests.test_api_routes` passed. Added `/bodesign/api/projects/{project_id}/knowledge/external-fetch`, which always returns `blocked-policy-gate`, `external_fetch_enabled=false`, and `requires_user_approval=true` until an explicit user-approved fetching policy exists.
- T25 KiCad bridge spike validation: `python3 -m compileall -f packages/eda-bridge services/api/main.py tests/test_api_routes.py` and API route tests passed. Added an adapter-bound KiCad integration plan contract for plugin/submodule workflows; bodesign records intended export/import/DRC steps but does not invoke native KiCad/freerouting tools directly in the server path.
- T26 reference-board workflow validation: `python3 -m compileall -f packages/workflow-core services/api/main.py tests/test_api_routes.py` and `PYTHONPATH="packages/shared:packages/design-ir:packages/component-knowledge:packages/component-kb:packages/doc-core:packages/source-core:packages/reverse-core:packages/gerber-core:packages/eda-bridge:packages/workflow-core:services/api" python3 -m unittest tests.test_api_routes` passed. Added workflow-core and `/bodesign/api/projects/{project_id}/workflow/reference-board`, returning a client-orchestrated MCP workflow plan with stages for ingest, knowledge resolution, IR reconstruction, layout intent proposal, deterministic validation, and user approval gates.
- T27 generated candidate workspace validation: `python3 -m compileall -f packages/workflow-core services/api/main.py tests/test_api_routes.py` and route tests passed. Added `/bodesign/api/projects/{project_id}/candidates/generated-design` plus a web Candidates tab showing candidate id, source IR, diff/evidence rows, validation gates, warnings, and explicit default `not-approved` state; no generated layout or send-to-fab output is produced.
- Document management/storage strategy checkpoint: user clarified the document management UI should be client-owned-folder first. Users manage all project documents in a local project folder structure; MCP analyzes/splits files and renders a web UI over that structure, can cache render/index artifacts, and may save edits back through a scoped storage share, but the client remains the durable owner of raw files, derived source chunks, datasheets, schematics, edits, and generated artifacts. This follows the docxmcp-style boundary and requires a client↔MCP storage-share contract before implementing durable project storage.
- Project folder UX checkpoint: user clarified that the local structure must not expose a large exploded machine folder tree. Root-level project folders should stay human-facing and industry-aligned around original inputs such as datasheets/reference/manufacturing packages and final outputs such as schematics, board drawings, reports, and generated candidates. Machine-only intermediates should be hidden under a system workspace such as `.bodesign/`, while the MCP web UI presents those internals as evidence views when needed.
- KiCad/EDA taxonomy checkpoint: user asked to deepen the plan with EDA-domain folder conventions. The human-facing root should preserve recognizable KiCad/EDA roles: `docs/` or `inputs/` for datasheets/reference/manufacturing packages, `eda/` for KiCad project/schematic/PCB files, `libraries/` for symbols/footprints/3D models/vendor libraries, `outputs/` for reviewed PDF/Gerber/drill/BOM/position/STEP/release artifacts, and `reports/` for human validation summaries. MCP-specific IR/chunks/cache/provenance/workflow state remains hidden under `.bodesign/`.
- KiCad Happy skill checkpoint: requested path `/home/pkcs12/projects/skills/kicad-happy` was not present; the available KiCad-related skills are `kicad`, `kidoc`, and `jlcpcb`. Relevant KiCad Happy conventions from `kicad/SKILL.md` include `.kicad-happy.json`, analyzer JSON under an `analysis/` run folder, `analysis/manifest.json`, `track_in_git=false`, trust summaries, and prior-run diffs. Plan update: bodesign should support these conventions but default MCP-triggered analyzer output to hidden `.bodesign/analysis/kicad-happy/` and expose it through web evidence views instead of cluttering the human-facing root.
- KiCad-first foundation checkpoint: user agreed the Gerber→circuit design source and OpenMV datasheet/reference→circuit design source paths remain challenging and should not lead immediate implementation. Decision: first make normal client-owned KiCad project usage work well — folder share, KiCad file/library/output detection, hidden analyzer cache, Web evidence browsing, and safe save-back — then use that foundation to connect the two harder reconstruction pipelines.
- KiCad-centered frontend checkpoint: user clarified that the Web frontend main axis also changes. The primary product UI should become a KiCad project workspace with Project, Schematic, PCB Layout, Libraries, Datasheets/Docs, Analysis, Manufacturing Outputs, Reports, and Candidate Review surfaces. Rockbox/Gerber/IPC/BOM/raster/reconstruction views remain as evidence and cross-probing panels inside that workspace, not the leading navigation model.
- T14e KiCad-centered web shell validation: coding subagent refactored the bodesign web shell so primary tabs are Project Overview, Schematic, PCB Layout, Libraries, Datasheets/Docs, Analysis, Manufacturing Outputs, Reports, and Candidate Review while Rockbox/Gerber/raster/reconstruction content is evidence context instead of the main axis. Orchestrator verification passed with `python3 -m compileall -f services/api/main.py tests/test_api_routes.py` and `PYTHONPATH="packages/shared:packages/design-ir:packages/component-knowledge:packages/component-kb:packages/doc-core:packages/source-core:packages/reverse-core:packages/gerber-core:packages/eda-bridge:packages/workflow-core:services/api" python3 -m unittest tests.test_api_routes`. Architecture Sync: Verified; `specs/architecture.md` already records the frontend axis pivot.
- T14a storage-share manifest validation: coding subagent added `packages/storage-core` with a client-owned storage-share manifest contract, default manifest builder, validation helper, and API route `/bodesign/api/projects/{project_id}/storage-share`. The manifest records `durable_owner=client`, hidden `.bodesign` workspace, semantic human-facing folders, disposable MCP cache policy, scoped read/write permissions, client-approved save-back mode, and conflict policy. Orchestrator verification passed with `python3 -m compileall -f packages/storage-core services/api/main.py tests/test_api_routes.py` and `PYTHONPATH="packages/shared:packages/design-ir:packages/component-knowledge:packages/component-kb:packages/doc-core:packages/source-core:packages/reverse-core:packages/gerber-core:packages/eda-bridge:packages/workflow-core:packages/storage-core:services/api" python3 -m unittest tests.test_api_routes`. Architecture Sync: Verified; storage-share principles are already captured in `specs/architecture.md`.
- T14b KiCad/EDA folder taxonomy validation: coding subagent extended `packages/storage-core` with deterministic taxonomy classification for relative paths. The helper classifies human-facing `docs/`/`inputs/`, `eda/`, `libraries/`, `outputs/`, and `reports/`, detects KiCad source files (`.kicad_pro`, `.kicad_sch`, `.kicad_pcb`), recognizes manufacturing/release outputs, and excludes hidden `.bodesign` machine workspace paths from root taxonomy. Orchestrator verification passed with `python3 -m compileall -f packages/storage-core services/api/main.py tests/test_storage_core.py tests/test_api_routes.py` and `PYTHONPATH="packages/shared:packages/design-ir:packages/component-knowledge:packages/component-kb:packages/doc-core:packages/source-core:packages/reverse-core:packages/gerber-core:packages/eda-bridge:packages/workflow-core:packages/storage-core:services/api" python3 -m unittest tests.test_storage_core tests.test_api_routes`. Architecture Sync: Verified; folder taxonomy principles are already captured in `specs/architecture.md`.
- T14c KiCad Happy hidden cache validation: coding subagent added a KiCad Happy cache mapping contract to `packages/storage-core`. It preserves `.kicad-happy.json` as a compatibility config, defaults MCP-run analysis outputs to `.bodesign/analysis/kicad-happy/`, keeps `track_in_git=false`, models manifest/analyzer JSON/trust summary/diffs/renders/report figures/DRC/ERC/DFM/EMC/thermal evidence paths, and supports visible `analysis/` only as an explicit opt-in compatibility mode. Orchestrator verification passed with `python3 -m compileall -f packages/storage-core services/api/main.py tests/test_storage_core.py tests/test_api_routes.py` and `PYTHONPATH="packages/shared:packages/design-ir:packages/component-knowledge:packages/component-kb:packages/doc-core:packages/source-core:packages/reverse-core:packages/gerber-core:packages/eda-bridge:packages/workflow-core:packages/storage-core:services/api" python3 -m unittest tests.test_storage_core tests.test_api_routes`. Architecture Sync: Verified; KiCad Happy integration principles are already captured in `specs/architecture.md`.
- T14d KiCad foundation API/UI validation: coding subagent connected storage-share, folder taxonomy, and KiCad Happy cache mapping into `/bodesign/api/projects/{project_id}/kicad-foundation` and the KiCad-centered web workspace. The foundation summary shows client-owned project folder status, detected KiCad sources/taxonomy roles, hidden analysis cache, safe save-back policy, and explicit blockers for real client folder browsing/write-back; it does not execute KiCad or resume Gerber/datasheet reconstruction paths. Orchestrator verification passed with `python3 -m compileall -f packages/storage-core services/api/main.py tests/test_storage_core.py tests/test_api_routes.py` and `PYTHONPATH="packages/shared:packages/design-ir:packages/component-knowledge:packages/component-kb:packages/doc-core:packages/source-core:packages/reverse-core:packages/gerber-core:packages/eda-bridge:packages/workflow-core:packages/storage-core:services/api" python3 -m unittest tests.test_storage_core tests.test_api_routes`. Architecture Sync: Verified; `specs/architecture.md` already records KiCad-first foundation and frontend-axis constraints.
- KiCad-native integration correction: user clarified that bodesign should not build a browser KiCad clone. Decision: native KiCad should own schematic/PCB editors, canvas interaction, libraries, and DRC/ERC; bodesign should become a KiCad companion/extension layer with MCP/API sidecar, evidence dashboard, document management, AI workflow, candidate review, and approved patch/save-back semantics. Plan update: T14d/T14e are downgraded to partial until they expose this native-boundary clearly, and new T14f defines the KiCad Action Plugin/sidecar integration contract.
- T14d/T14e/T14f KiCad-native rework validation: implementation now exposes `/bodesign/api/projects/{project_id}/kicad-native-extension`, adds an eda-bridge `KiCadNativeExtensionContract`, nests it into the foundation summary, and rewrites the Web shell as a KiCad companion dashboard. The UI explicitly says native KiCad owns schematic/PCB editing, canvas, libraries, DRC, and ERC; bodesign Web presents evidence/status, document management, analysis cache, candidate review, and approval gates. Validation passed with `python3 -m compileall -f packages/eda-bridge services/api/main.py tests/test_eda_bridge.py tests/test_api_routes.py` and `PYTHONPATH="packages/shared:packages/design-ir:packages/component-knowledge:packages/component-kb:packages/doc-core:packages/source-core:packages/reverse-core:packages/gerber-core:packages/eda-bridge:packages/workflow-core:packages/storage-core:services/api" python3 -m unittest tests.test_eda_bridge tests.test_api_routes`. Architecture Sync: Updated to KiCad-native foundation and companion-dashboard boundary.
- T14g planning checkpoint: after committing the KiCad-native foundation slice, the next step is a minimal KiCad Action Plugin/sidecar scaffold. It should not execute DRC/ERC or mutate project files yet; it should define the native entrypoint, sidecar URL/config, active project discovery, open-dashboard/request-analysis actions, and an explicit approved-patch-only save-back path.
- T14g KiCad Action Plugin scaffold validation: coding subagent added `packages/kicad-plugin` and `tests/test_kicad_plugin.py`. The scaffold imports without KiCad/`pcbnew`, exposes plugin metadata, builds default bodesign sidecar URLs, discovers active project context from `.kicad_pro`/`.kicad_sch`/`.kicad_pcb` paths, represents request-analysis calls, and blocks unapproved patch requests while representing approved patch requests without applying files. Orchestrator verification passed with `python3 -m compileall -f packages/kicad-plugin tests/test_kicad_plugin.py` and `PYTHONPATH="packages/kicad-plugin" python3 -m unittest tests.test_kicad_plugin`. Architecture Sync: Added plugin scaffold as the native KiCad entrypoint under the KiCad-native foundation gate.
- T14h KiCad plugin/sidecar handshake validation: added a represented plugin handshake request builder and `/bodesign/api/projects/{project_id}/kicad-plugin-handshake`. The handshake returns sidecar availability, dashboard/foundation/native-extension/request-analysis URLs, approved read/request capabilities, blocked native/file-mutation capabilities, and explicit approved-patch-only policy without running KiCad, DRC/ERC, or writing files. Validation target: `python3 -m compileall -f packages/kicad-plugin services/api/main.py tests/test_kicad_plugin.py tests/test_api_routes.py` plus `PYTHONPATH="packages/kicad-plugin:packages/shared:packages/design-ir:packages/component-knowledge:packages/component-kb:packages/doc-core:packages/source-core:packages/reverse-core:packages/gerber-core:packages/eda-bridge:packages/workflow-core:packages/storage-core:services/api" python3 -m unittest tests.test_kicad_plugin tests.test_api_routes`. Architecture Sync: Extended plugin scaffold into an explicit sidecar readiness handshake.
- T14i planning checkpoint: next KiCad foundation slice should expose a client-owned folder tree/evidence browse contract. It must derive from the storage-share manifest/taxonomy, present human-facing KiCad/EDA folders plus hidden `.bodesign` cache summaries in Web, and remain scoped/read-only by default; bodesign must not become an arbitrary filesystem owner or mutate client files without a later approved save-back workflow.
- T14i client-owned project tree validation: added `ProjectTreeBrowseContract` in storage-core, `/bodesign/api/projects/{project_id}/project-tree`, and a companion dashboard project-tree panel. The tree is `read-only-fixture-backed`, derives from storage-share manifest/taxonomy paths, lists `docs`/`inputs`, `eda`, `libraries`, `outputs`, and `reports` nodes with counts/samples, summarizes hidden `.bodesign` evidence/cache, exposes no mutation capabilities, and blocks arbitrary server filesystem browsing. Validation passed with `python3 -m compileall -f packages/storage-core services/api/main.py tests/test_storage_core.py tests/test_api_routes.py` and `PYTHONPATH="packages/shared:packages/design-ir:packages/component-knowledge:packages/component-kb:packages/doc-core:packages/source-core:packages/reverse-core:packages/gerber-core:packages/eda-bridge:packages/workflow-core:packages/storage-core:services/api" python3 -m unittest tests.test_storage_core tests.test_api_routes`.
- T13a planning checkpoint: next client-owned foundation slice should replace the single Rockbox fixture assumption with a project registry contract. The registry should list client-owned project records, link each record to storage-share/project-tree/KiCad foundation/native plugin surfaces, expose folder-handle readiness/blockers, and remain fixture-backed/read-only until a client grants real folder handles.
- T13a client-owned project registry validation: added `ProjectRegistry`/`ProjectRecord` contracts in storage-core, changed `/bodesign/api/projects` to return a registry envelope, and updated the companion dashboard with registry status/access/owner context. Records include project id, display name, durable owner `client`, `fixture-not-granted` folder-handle status, read-only fixture access, links to dashboard/storage-share/project-tree/KiCad foundation/native extension/plugin handshake, and blockers for client handles/save-back. Validation passed with `python3 -m compileall -f packages/storage-core services/api/main.py tests/test_storage_core.py tests/test_api_routes.py` and `PYTHONPATH="packages/shared:packages/design-ir:packages/component-knowledge:packages/component-kb:packages/doc-core:packages/source-core:packages/reverse-core:packages/gerber-core:packages/eda-bridge:packages/workflow-core:packages/storage-core:services/api" python3 -m unittest tests.test_storage_core tests.test_api_routes`.
- T14j planning checkpoint: next storage-share slice should model the client folder open/import request rather than reading arbitrary paths. The contract should state requested read/write scopes, folder-handle approval state, client-side grant requirements, fail-fast blockers, and post-grant actions for refreshing project registry/tree/foundation surfaces.
- T14j client folder open/import request validation: added `FolderOpenRequest` in storage-core, `/bodesign/api/projects/{project_id}/folder-open-request` as GET/POST represented request endpoints, and an overview panel for folder-open status. The request defaults to `needs-client-grant/not-approved`, `durable_owner=client`, `access_mode=no-server-filesystem-access`, carries requested read/save-back representation permissions, returns read/write scopes from the storage-share manifest, lists post-grant refresh actions, exposes no mutation capabilities, and does not scan server filesystem paths. Validation passed with `python3 -m compileall -f packages/storage-core services/api/main.py tests/test_storage_core.py tests/test_api_routes.py` and `PYTHONPATH="packages/shared:packages/design-ir:packages/component-knowledge:packages/component-kb:packages/doc-core:packages/source-core:packages/reverse-core:packages/gerber-core:packages/eda-bridge:packages/workflow-core:packages/storage-core:services/api" python3 -m unittest tests.test_storage_core tests.test_api_routes`.
- T14k planning checkpoint: next storage-share slice should model save-back as a client-applied proposal. Proposed edits must carry target scopes, evidence refs, approval state, conflict policy, and client/native KiCad application instructions; MCP-side direct file mutation remains blocked until explicit user-approved save-back exists.
- T14k client-applied save-back proposal validation: added `SaveBackProposal` in storage-core, `/bodesign/api/projects/{project_id}/save-back-proposals` as GET/POST represented proposal endpoints, and an overview panel for save-back status. The fixture proposal targets `reports/bodesign-analysis-summary.md`, defaults to `not-approved`, uses `client-applied/native-kicad-plugin`, carries evidence refs and conflict policy, blocks direct MCP mutation, exposes no mutation capabilities, and does not write or apply patches.
- T14l planning checkpoint: next storage-share slice should expose cache invalidation and conflict status. It should represent cache freshness, source revision anchors, conflict policy, and required client refresh/approval actions without reading arbitrary files, resolving conflicts silently, or turning MCP cache into durable project storage.
- T14l cache invalidation/conflict status validation: added `CacheConflictStatus` in storage-core, `/bodesign/api/projects/{project_id}/cache-conflict-status`, and an overview panel for cache/conflict state. The contract marks MCP cache as `disposable-mcp-cache`, client folder as source authority, freshness as `fixture-stale/needs-client-refresh`, conflict policy as `explicit-user-resolution`, blocks silent resolution, lists source revision anchors and required refresh/invalidate/save-back review actions, and performs no filesystem scan, cache deletion, or conflict resolution.
- T14m planning checkpoint: next storage-share slice should model source-chunk materialization for docxmcp-style PDF/text decomposition. It should represent provenance anchors, hidden `.bodesign/sources/` target locations, approval/cache state, and client-applied materialization instructions without copying client documents into server-owned storage.
- T14m source-chunk materialization validation: added `SourceChunkMaterialization`/`SourceChunkItem` in storage-core, `/bodesign/api/projects/{project_id}/source-chunks/materialization` as GET/POST represented materialization endpoints, and an overview panel for docxmcp/client orchestration. The contract uses `source_authority=client-owned-folder`, `target_workspace=.bodesign/sources`, `materialization_mode=client-applied/docxmcp-orchestration`, default not-approved state, provenance anchors, represented-not-materialized chunk items, direct server copy block, and performs no file reads/copies or server-owned storage writes.
- T14n planning checkpoint: next KiCad-native foundation slice should model analysis request/status for KiCad Happy/native plugin workflows. It should represent requested checks, approval state, hidden `.bodesign/analysis/kicad-happy/` cache targets, expected evidence outputs, current run status, and blockers while avoiding server-side KiCad/DRC/ERC/analyzer execution or file mutation.
- T14n KiCad analysis request/status validation: added `KiCadAnalysisStatus`/`KiCadAnalysisEvidenceOutput` in storage-core, `/bodesign/api/projects/{project_id}/kicad-analysis-status` as GET/POST represented analysis endpoints, plugin request-analysis URL targeting that endpoint, and overview/Analysis panels. The contract uses `native-kicad-plugin/client-orchestrated-kicad-happy`, default `not-approved/needs-client-grant`, `represented-not-run`, hidden `.bodesign/analysis/kicad-happy` outputs for manifest/analyzer/trust/diffs/DRC/ERC/DFM/EMC/thermal evidence, direct server execution block, and performs no KiCad/DRC/ERC/analyzer subprocess execution, filesystem access, or file mutation. Validation passed with `python3 -m compileall -f packages/storage-core packages/kicad-plugin services/api/main.py tests/test_storage_core.py tests/test_api_routes.py tests/test_kicad_plugin.py` and `PYTHONPATH="packages/kicad-plugin:packages/shared:packages/design-ir:packages/component-knowledge:packages/component-kb:packages/doc-core:packages/source-core:packages/reverse-core:packages/gerber-core:packages/eda-bridge:packages/workflow-core:packages/storage-core:services/api" python3 -m unittest tests.test_storage_core tests.test_api_routes tests.test_kicad_plugin`.
- T14o planning checkpoint: next analysis slice should model a KiCad Happy evidence manifest after client/native plugin execution. It should index hidden-cache outputs such as manifest, analyzer JSON, trust summary, diffs, renders, reports, DRC/ERC/DFM/EMC/thermal evidence, freshness, and blockers without reading arbitrary files or treating cache artifacts as authoritative source data.
- Session handoff checkpoint: a new operator took over after T14o. Assessment found the A2 KiCad-native contract series (T14a–T14o) saturated as deterministic "represented-not-run" fixtures and chose to advance real reconstruction capability (T18/T20) over stacking more representational contracts. Empirical validation on the Rockbox fixture established that Excellon drill X/Y (`raw/1000`) and IPC-D-356A 317/327 X/Y (`raw/100`) resolve to the same physical point (max residual ≤0.01 fusion units across a 400-hit sample), so drill↔via spatial matching is exact; the Allegro `cds2f` placement export uses a different origin (non-constant ~1607 fusion-unit centroid offset) and is therefore left uncoregistered rather than faked.
- T18/T20 spatial drill↔via fusion checkpoint: added a first real `GeometryPrimitive` IR type in design-ir and a `packages/reverse-core/fusion.py` spatial fuser. It parses IPC 317/327 features with coordinates, normalizes drill hits to the shared IPC/drill frame, matches each plated drill hit to its nearest IPC via within tolerance, classifies unmatched holes as tooling/mounting, assigns nets to drill holes, and emits net-bearing via `GeometryPrimitive`s. `/bodesign/api/projects/{project_id}/geometry` now returns a `spatial_fusion` envelope and Board View shows a drill↔via card plus a per-net via census. Rockbox result: 817 IPC vias, 938 pads, 789 drill hits, 783 matched (ratio 0.9924), 6 unmatched, 92 distinct via nets (GND dominant at 598). This is true spatial reconstruction evidence, not a represented contract.
- T14o KiCad analysis evidence manifest validation: added `KiCadAnalysisEvidenceManifest`/`KiCadAnalysisEvidenceArtifact` in storage-core, `/bodesign/api/projects/{project_id}/kicad-analysis-evidence`, and companion dashboard/Analysis panels. The contract represents manifest/analyzer/trust/diffs/renders/reports/DRC/ERC/DFM/EMC/thermal evidence under hidden `.bodesign/analysis/kicad-happy`, marks cache as disposable/read-only/fixture-backed, reports freshness/blockers/source anchors, and performs no filesystem browsing, native execution, cache deletion, or file mutation. Validation passed with `python3 -m compileall -f packages/storage-core services/api/main.py tests/test_storage_core.py tests/test_api_routes.py` and `PYTHONPATH="packages/shared:packages/design-ir:packages/component-knowledge:packages/component-kb:packages/doc-core:packages/source-core:packages/reverse-core:packages/gerber-core:packages/eda-bridge:packages/workflow-core:packages/storage-core:services/api" python3 -m unittest tests.test_storage_core tests.test_api_routes`.
- Forward-design north-star checkpoint: user reframed the long-term product as a "vibe coding" circuit-design MCP tool — NL spec (e.g. "STM32N6 + Nordic nRF9151 NPU compute interface with comms, 8GB flash, USB-C power/charging, internal 18650 battery + control board") → AI clarifying dialogue → planning → emit a KiCad design. Two decisions taken: (1) build the EDA bridge real, starting with A-emit (emit KiCad-openable files) over the in-KiCad plugin, and **install KiCad on this machine** so the loop can be validated end-to-end; (2) reuse the existing skill ecosystem (bom / digikey / mouser / lcsc / datasheets / kicad / spice / emc) as the parts/pinout/simulation backend rather than building a native component DB. User further clarified the AI does **not** design from scratch: it starts from the target chips' dev-board datasheets / reference designs and composes subsystems (Milestone G). Honest scoping recorded: KiCad's schematic editor has essentially no scripting API, so generation is done by emitting `.kicad_sch` S-expressions (SKIDL-style) validated by `kicad-cli`; full auto place-and-route to a finished layout is out of near-term scope.
- KiCad install checkpoint: installed KiCad 9.0.9 from `ppa:kicad/kicad-9.0-releases` on Ubuntu 24.04 plus `kicad-symbols` (223 libs) and `kicad-footprints` (155 libs). `kicad-cli` exposes `sch erc`, `sch export` (netlist/pdf/bom), `pcb drc`, and `pcb export`. Coverage finding that validates the reference-design strategy: **STM32N6 is absent from KiCad's standard symbol libraries** (newest ST family is H5/H7), while nRF9151/nRF9160 and USB-C connectors are present — so flagship-new parts need datasheet-pinout-driven symbol/footprint generation (T29). GPL posture clarified: `kicad-cli` is used as an external subprocess over the open `.kicad_sch` format, not GPL source linked into the product.
- T28 A-emit checkpoint: spiked the exact KiCad 9 `.kicad_sch` format against the real `kicad-cli`, proving a self-contained recipe — embed used symbol defs in `lib_symbols`, place symbol instances with per-symbol `instances` blocks, and connect pins via `global_label`s placed at pin endpoints `(Sx+px, Sy−py)` (library Y-flip), with no wire-routing geometry required. Promoted into `packages/eda-bridge/kicad_emit.py` with `load_symbol`, `emit_kicad_schematic`, and `validate_kicad_schematic` (fail-soft when `kicad-cli` is absent). This is the first real, non-represented EDA bridge capability.
- T18/T20 spatial drill↔via fusion validation: `python3 -m compileall -q packages services tests` and the full suite (`python3 -m unittest discover -s tests`, 57 tests, 1 skipped) passed; the venv run with pygerber available passed 32 targeted geometry/reconstruction tests including the live pygerber render. New `tests.test_rockbox_reconstruction` asserts the deterministic Rockbox fusion counts (817 vias, 938 pads, 789 hits, 783 matched, 6 unmatched, 92 via nets, GND-dominant census, via `GeometryPrimitive`s, and the explicit placement-frame warning) plus safe degradation with no evidence; `tests.test_api_routes` asserts the `spatial_fusion` geometry envelope (matched/unmatched/ratio/primitive count, no inlined primitives) and the Board View drill↔via panel. Live verification: `127.0.0.1:8765/bodesign/api/projects/rockbox/geometry` returned the spatial_fusion summary, `/bodesign/projects/rockbox` rendered the drill↔via card and per-net via census, and gateway `127.0.0.1:1080/bodesign/` returned 200. Architecture Sync: drill↔via spatial fusion documented as real geometry evidence; Gerber pad/flash spatial fusion and placement↔IPC origin co-registration remain pending.
- T28 A-emit validation: `python3 -m compileall -q packages services tests` and the full suite (`python3 -m unittest discover -s tests`, 61 tests, 1 skipped) passed. New `tests.test_kicad_emit` covers `load_symbol` pin endpoints, self-contained schematic emission with global-label connectivity, unresolved-pin recording for unknown symbols, and (when `kicad-cli` is present) a live ERC + netlist check asserting 0 ERC errors, 3 netlist components, and the exact `MID`={R1.2,R2.1,C1.1} / `GND`={R2.2,C1.2} connectivity. Fixed `tests.test_kicad_plugin` whose `assertFalse(PCBNEW_AVAILABLE)` had baked in the pre-install no-KiCad environment; it now asserts import-safety holds regardless of `pcbnew` presence (KiCad install made `pcbnew` importable). Architecture Sync: EDA bridge section updated from adapter-bound plan contract to a real IR→KiCad emitter validated by `kicad-cli`.
- OpenMV N6 datasheet→KiCad feasibility checkpoint: created `plans/product_openmv_datasheet_kicad_source/` with feasibility, tasks, and implementation spec. Public evidence found: OpenMV repo `boards/OPENMV_N6`, MCU `STM32N657xx`, J-Link target `STM32N657L0`, OpenMV product handle `openmv-n6`/SKU `OMV-N6-V1`, firmware evidence for USB HS, CSI/DCMI, I2C/SPI, audio, IMU, display, XSPI PSRAM, and probable `MX25UM51245G` flash. User then identified the real PDF corpus under GDrive `@利善美/03.研發資料/02.OpenMV/`; inventory found `OpenMV-N6-Schematic-Rev4.pdf`, STM32N657L0 datasheet/reference manual, MX25UM25645G flash, APS512XXN PSRAM, BQ24075 charger, LM66100/LM66200 ideal diodes, TPS62824/TPS62826/MIC5365/TCR2LE33/AP22800 power parts, CYW43439/LBEE5KL1YN wireless, RTL8211FI Ethernet, EMI/TVS filters, LSM6DSM IMU, MMICT3902 mic, MCP6006T opamp, SD-card switches, and `OpenMV N6.url`. Plan updated to mark O0 complete and treat this GDrive folder as client-owned source authority. Feasibility: reusable KiCad source package and schematic-level extraction are now plausible; exact PCB layout clone remains blocked without PCB source/manufacturing outputs.
- Board-design scope correction: user clarified firmware is developed separately and should not burden the Board Design phase. Plan updated so OpenMV firmware repository/config files are optional cross-check evidence only; source authority for KiCad generation is the GDrive schematic/datasheet corpus, especially `OpenMV-N6-Schematic-Rev4.pdf` plus component datasheets. Firmware loader mismatch (`MX25UM51245G` vs local `MX25UM25645G` datasheet) is no longer a Board Design blocker unless schematic/BOM evidence conflicts.
- OpenMV O1 source manifest checkpoint: created `plans/product_openmv_datasheet_kicad_source/source-manifest.json` with 26 client-owned GDrive sources, SHA-256 anchors, byte sizes, document roles, trust grades, fetch status, license/redistribution posture, extraction state, and the OpenMV N6 product URL shortcut. Policy remains `do-not-redistribute-raw-pdfs`; raw PDFs stay under the client-owned GDrive folder. Validation: manifest is valid JSON and records all 26 files from `/home/pkcs12/projects/documents/gdrive/@利善美/03.研發資料/02.OpenMV/`. Gap noted: `TPS62824` and `TPS62826` local PDFs currently have identical SHA-256 bytes and should be verified during O2/O6.
- OpenMV O2 STM32N657 source chunk checkpoint: created `plans/product_openmv_datasheet_kicad_source/stm32n657-source-chunks.json` as a metadata-only provenance chunk manifest for `Processor Datasheet STM32N657L0.pdf` and `Reference Manual STM32N657L0.pdf`. Validation used `pdfinfo`, `pdftotext`, and `jq`: datasheet page count is 258, reference manual page count is 4691, SHA-256 anchors match `source-manifest.json`, and datasheet TOC/search locates pinout/ballout at pages 83-88, pin description starting page 89, and package mechanical pages 237-254. Raw PDF text/table extraction remains client-approved materialization under `.bodesign/sources`, not repo content.
- OpenMV O3 component-knowledge seed checkpoint: created `plans/product_openmv_datasheet_kicad_source/stm32n657-component-knowledge.json`. Schematic text extraction found `stm32n657.SchDoc` and U5 BGA coordinate labels; unique `PIU50<ball>` coordinates count to 223, matching the datasheet VFBGA223/B0GK package family. The seed records package evidence, representative observed USB/CSI/boot-reset signals, and project-local KiCad library targets (`libraries/symbols/openmv_generated.kicad_sym`, `libraries/footprints/openmv_generated.pretty/`). Stop gate preserved: symbol generation remains blocked because the full 223-row pin name/type/electrical-role table has not been materialized or parsed.
- OpenMV O3a planning checkpoint: user confirmed the missing toolchain should be built before symbol generation. O3a is now the required gate between the VFBGA223 package seed and O4: develop a client-owned PDF table extractor adapter, ST pin-table normalizer, evidence mapper, and validation gate that emits `stm32n657-vfbga223-pin-table.json` only after all 223 VFBGA223 balls have verified names/types/functions/source refs. O4 remains blocked; project-local KiCad libraries are allowed, global KiCad library/config mutation remains forbidden.
- OpenMV O3a/O3b implementation checkpoint: added `bodesign_doc_core.pin_tables` with `pdftotext` page-span extraction, ST tokenized pin-table normalization for VFBGA223, per-row source/page/table evidence refs, validation, and gap-report generation. Initial live extraction correctly blocked at 221/223, then AI-assisted review identified PDF table-layout edge cases: blank-name `OSC32_IN/OSC32_OUT` rows and wrapped `PB4/PB5` names. After tool adjustment plus explicit review artifact, live validation produced `stm32n657-vfbga223-pin-table.json` with 223 unique balls, no duplicates, required reset/boot/power pins present, and no raw PDF text committed. `stm32n657-pin-table-ai-review.json` records that tools extract/validate while AI adjudicates ambiguous source layout; O4 may proceed using this accepted project-local pin table.
- OpenMV O4 project-local KiCad symbol checkpoint: added `bodesign_eda_bridge.kicad_symbol` to convert the accepted STM32N657 VFBGA223 pin table into `plans/product_openmv_datasheet_kicad_source/libraries/symbols/openmv_generated.kicad_sym`. The symbol has 223 pins, includes key reviewed pins (`NRST`, `BOOT0`, `OSC32_IN`, `OSC32_OUT`, `PB4`, `PB5`), carries `BodesignEvidence` metadata instead of raw PDF text, and does not mutate global KiCad libraries or library tables. Validation: `tests.test_eda_bridge` passes and `kicad-cli sym export svg --symbol STM32N657L0_VFBGA223` renders the generated symbol, proving KiCad can parse it.
- OpenMV O5 external flash checkpoint: created `plans/product_openmv_datasheet_kicad_source/mx25um25645g-component-knowledge.json` from client-owned schematic/datasheet evidence only. Schematic page 7 identifies U7 as `MX25UM25645GXDI00` / `32MB FLASH`; Macronix datasheet evidence identifies `MX25UM25645G` as 1.8 V 256 Mbit octal flash, `XD` 24-Ball BGA package, 200 MHz 24-BGA octal I/O support, and 1.65-2.0 V VCC/VCCQ operating range. The artifact maps XSPIM_P2 IO0-IO7, DQS, CS#, SCLK, RESET#, power and ground pins, records schematic routing constraints, keeps firmware naming as non-authority, and preserves gaps for alternative-footprint NC/DNU pins plus footprint geometry review. Validation: `jq` checks source hashes, part ID, 24 pin records, and required XSPIM_P2 nets; no raw PDF text was committed.
- OpenMV O6 subsystem constraints checkpoint: created `plans/product_openmv_datasheet_kicad_source/openmv-n6-subsystem-constraints.json` from client-owned schematic/datasheet evidence. It covers the required subsystem IDs (`usb_hs`, `xspi_flash`, `camera_csi`, `i2c`, `spi_display`, `audio`, `imu`, `power`, `storage`, `wireless`, `ethernet`, `protection`) with concise source anchors, key components, nets/signals, electrical/routing/power constraints, confidence, and gaps. Low-confidence areas such as SPI display identity, PSRAM normalization, audio format, RF geometry, RGMII timing, SD DAT2/DAT3, and protection capacitance/clamping remain explicit gaps. Validation: `jq` confirmed all required subsystem IDs are present, raw PDF text is not committed, firmware remains non-authority, and each subsystem has evidence/gap structure.
- OpenMV O7 minimal KiCad subsystem schematic checkpoint: added deterministic `bodesign_eda_bridge.openmv_subsystem` and generated `plans/product_openmv_datasheet_kicad_source/generated/openmv_n6_subsystem/openmv_n6_subsystem.kicad_sch` plus `.kicad_pro`. The schematic embeds project-local STM32N657 and MX25UM25645G symbols, connects verified XSPIM_P2 IO0-IO7/DQS/NCS/CLK/RESET nets plus 1.8 V/GND flash rails, carries evidence metadata, avoids global KiCad library/table mutation, and stores no raw PDF text. Validation: `tests.test_eda_bridge` passes, `kicad-cli sch export netlist` loads the schematic and emits a netlist containing the required nets. ERC reports expected violations because this is an intentionally incomplete subsystem source artifact, not a finished electrical design.

- Plan reorganization checkpoint (2026-06-06): a new operator took over and re-sequenced the plan around the forward vibe-design north star. Plan hygiene fixed: main `.state.json` moved `planned → implementing` (approval already granted, deep in implementation), and the OpenMV track gained a `.state.json` (`implementing`, O8 remaining, parent `product_pcb_ai_viewer`). A "Reorganized Roadmap" section now leads `product_pcb_ai_viewer/tasks.md` as the authoritative forward plan (R1–R10 across P0 close-the-OpenMV-slice, P1 vibe front-end, P2 generalize, P3 round-trip/finish), with the A2 represented contracts and the reverse-path Gerber geometry (B) explicitly parked as secondary. Rationale: the product's center of gravity has moved from reverse-engineering Rockbox Gerbers to forward, reference-design-grounded KiCad generation; the OpenMV O0–O7 work proved that path end-to-end on a flagship-new chip (STM32N657, absent from KiCad's standard libraries), so the immediate priority is to close/harden that slice (R1–R4) before building the vibe front-end (R5) and generalizing (R7). The detailed `T*`/`O*` ledger is preserved as history. Build health at handoff: 65 tests pass (1 skipped), KiCad 9.0.9 installed.

- R1/O8 gap report validation: `python3 -m compileall -q packages tests` and the full suite (68 tests, 1 skipped) passed. Added `bodesign_workflow_core.gap_report` (`collect_source_gap_report` + `render_gap_report_markdown`) and `tests.test_gap_report`, which assert a blocking artifact yields readiness `blocked`, a stale `blocked_symbol_generation` seed is downgraded to `resolved-superseded` once an `o4_unblocked`/validated-pin-table signal exists, and the real OpenMV package reports `reusable-as-source-evidence-with-gaps` with 0 blocking gaps and the validated 223-ball pin table surfaced as a resolved fact. Generated `plans/product_openmv_datasheet_kicad_source/reports/openmv_n6_source_gaps.md` and `openmv-n6-evidence-dashboard.json` (0 blocking, 16 open, 4 resolved facts). The `dashboard_data` is the structured feed for the R4 web evidence dashboard.

- R4 OpenMV evidence dashboard validation: added `/bodesign/api/openmv/evidence` (live `collect_source_gap_report` over the package artifacts) and `/bodesign/openmv` (HTML evidence page: readiness badge, resolved facts, open gaps by area, generated KiCad source files, per-artifact summary), linked from the companion-dashboard sidebar. Caught and fixed a self-ingestion bug found in live testing: the endpoint globbed `*.json` and re-read its own generated `openmv-n6-evidence-dashboard.json` plus the new `.state.json`, inflating counts to 10 artifacts/33 open; now excludes those non-evidence files (`OPENMV_NON_EVIDENCE_JSON`), and a test locks `artifacts == 8`. Full suite 71 tests pass (1 skipped). Live: API and HTML return readiness `reusable-as-source-evidence-with-gaps` (8 artifacts, 16 open, 0 blocking); gateway `127.0.0.1:1080/bodesign/openmv` returned 200.

- R2 power-backbone validation: expanded `eda-bridge.openmv_subsystem` to connect the MCU's voltage-unambiguous power balls to named rails (`VDDA18*`→VCC_1.8V, `VDD33USB`→VCC_3.3V, `VSS*`/`VREF-`→GND, `VBAT`→V_BATT) and add 4 `Device:C` decoupling caps, taking the generated subsystem schematic from 2 to 6 components. Ambiguous IO/core power pins (VDD, VDDIO2/3/4, VDDCORE, VDDSMPS, VDDCSI, VREF+) are intentionally left unconnected and recorded as a warning, and USB/peripheral subsystem emit is deferred (no project-library symbols for the connector/regulator/PHY ICs and the MCU USB data balls are unverified — per the no-guessed-pins stop gate). Also fixed a latent global-label UUID collision (same-net labels on one part now get coordinate-derived UUIDs). `kicad-cli sch export netlist` validates the regenerated schematic with exact rail node counts GND=25 (18 MCU grounds + 4 cap pins + 3 flash), VCC_1.8V=8 (6 VDDA18* + 2 caps), VCC_3.3V=3, V_BATT=1. `tests.test_eda_bridge` updated to assert 6 components, the new rail labels, `Device:C`, and the deferred-subsystem warnings; full suite 71 tests pass (1 skipped).

- R3 footprint-map validation: added `eda-bridge.footprint_map` (deterministic package→footprint matcher over the installed `Package_BGA` library, scoring by ball-count/array/pitch/body with a match threshold and explicit gaps) plus `openmv_package_queries`. Generated `plans/product_openmv_datasheet_kicad_source/libraries/footprint-map.json` (project-local, under `libraries/` so the root evidence glob still sees only the 8 evidence artifacts). Honest output: MX25 24-BGA 5x5 → `candidate-match-needs-verification` best `BGA-24_6x8mm_Layout5x5_P1.0mm`; STM32N657 VFBGA223 → `no-stdlib-footprint-gap` (no 223-ball footprint installed; nearest ST 10x10/0.5mm parts are informational only). Surfaced in `/bodesign/openmv` (live `footprint_map` block in the evidence API + an HTML "Package → footprint map" table; file listed in the source-files table). `tests.test_footprint_map` + extended `tests.test_api_routes` lock the match/gap behaviour; full suite 74 tests pass (1 skipped). Live + gateway 200 confirmed. This completes P0 (R1–R4) for the OpenMV forward-design slice.

- R4b frontend hardening (user feedback): user reported the web UI looked mostly like placeholders with no hierarchy / information overload, and asked whether KiCad has a more mature frontend. Answer recorded: native KiCad desktop is the mature editor (our companion boundary); KiCanvas is the mature web *viewer* but is alpha and its KiCad-9 format support is unverified; the robust choice is `kicad-cli sch export svg` (KiCad's own engine, offline, zero JS). Implemented: `GET /bodesign/api/openmv/schematic.svg` renders the generated `openmv_n6_subsystem.kicad_sch` to SVG (cached under `.artifacts/openmv-schematic/`, re-rendered only when the source is newer) and the `/bodesign/openmv` page embeds it as the hero — the first genuinely real, visual artifact in the UI. Decluttered the Rockbox workspace: added a top hero linking to the real forward-design output and collapsed the parked storage/analysis represented-contracts (folder-open/save-back/cache-conflict/source-chunk/analysis-status/analysis-evidence) into a `<details>`. Added a `Response` shim to the test stub; `tests.test_api_routes` now asserts the SVG route returns `image/svg+xml`, the OpenMV page embeds the schematic, and the workspace renders the hero + parked section. Live: schematic.svg returns 200 (2.4 MB image/svg+xml); gateway 200. Suite: 76 tests pass (1 skipped).

- R5 requirement→planning loop validation (the "vibe" front end / T30): added `workflow-core.requirement_planning.plan_design_intent` — a deterministic, rule-based intake that extracts requirements from a natural-language spec, asks clarifying questions for missing fields, and decomposes the request into reference-design-grounded subsystems. Detection keywords are bilingual (EN + zh-TW/zh-CN) because specs arrive in Chinese — a test run on the user's spec initially missed `power_input`/`charging` (written 供電/充電, "usb typeC") until Chinese keywords were added; it now detects all 6 functional areas. `POST /bodesign/api/design-intent/plan` returns the plan; `GET /bodesign/design` is an interactive page (textarea → fetch → requirements/questions/subsystems) linked from the workspace hero. `tests.test_api_routes` asserts extraction+questions on the Chinese spec, that answers close the questions (status → `planned`), and route/page registration. Live: POST returns 5 subsystems + 3 questions; gateway 200. Suite: 79 tests pass (1 skipped). This is the rule-based skeleton of the vibe loop; an LLM can enrich extraction/questions later without changing the contract.

- ROCKBOX corpus update (2026-06-06): user reported the client ROCKBOX folder (`/home/pkcs12/projects/documents/gdrive/@利善美/03.研發資料/01.ROCKBOX`) now holds a full product document set — C01 ID, C02 ME, **C03 circuit design (OrCAD `ROCKBOX_DVT_..._IX.DSN` + `.png`, `ROCKBOX_DVT_..._BOM.xlsx`, and a `MDBT53-P1M & nRF9151 GPIOs` table)**, C04 Layout, C05 FW, C06 verification, C07 PRD, plus stackup. Implication: Rockbox no longer needs Gerber reverse-engineering; it can use the same forward/document path as OpenMV, so the reverse-path (B) is effectively retired (kept parked). Rockbox becomes a candidate second forward-design exemplar and notably shares nRF9151 with the running example spec.
- R6 evidence sourcing validation: added `workflow-core.evidence_sourcing` (`extract_part_candidates` + `build_design_evidence_manifest`) and `POST /bodesign/api/design-intent/evidence` with an "Evidence sourcing" section on `/bodesign/design`. Local-first: scans a caller-provided corpus dir read-only for per-part datasheet/schematic/BOM/pinout/PRD matches with role + trust grading; parts with no local match get policy-gated distributor pointers (digikey/lcsc), and actual fetch/skill execution stays agent-side. Verified live against the real ROCKBOX corpus: from spec "MDBT53-P1M and nRF9151 with 18650 battery" → nRF9151 + MDBT53-P1M graded A (matched the GPIO pinout table), 18650 → needs-sourcing. `tests.test_api_routes` locks local-evidence vs needs-sourcing behaviour on a synthetic corpus. Full suite 80 tests pass (1 skipped); gateway 200. This completes P1 (R5 vibe front end + R6 evidence sourcing); R7 (compose subsystems → IR → emit) is the next link to a generated schematic for an arbitrary spec.

- Product redefinition (2026-06-06): user reframed the end goal from a generic tool to a concrete target product — an **Edge AI device** (Nordic nRF9151 cellular + STM32N6 NPU core + camera module + microphone module + built-in battery + USB-C charging), whose document set must mirror 01.ROCKBOX's C01–C07 architecture, with bodesign driving it until a complete package is submittable to a manufacturer for POC/EVT/DVT. Three framing clarifications captured: (a) this is "build the tool while completing a real goal," not tooling for its own sake; (b) bodesign has a capability boundary, and the AI must *teach* the user how to prepare everything needed for outsourced test/verification and other processes; (c) the schematic/Gerber/layout are not AI-manufactured — they are produced by engineers in proper tools and handed to the factory. Conclusion: **bodesign = a document-package co-pilot** — harvest reference designs from the two corpora, generate *draft* electronics artifacts as accelerators, and (headline) teach/organize the C01–C07 + external-process readiness. Decisions: electronics-core ownership scope (C03/C04-constraints/C06-plan/C07; C01/C02/C05 tracked external), POC as the first milestone. Key feasibility win: `build_design_evidence_manifest` over `03.研發資料/` (both products) shows the new device's subsystems are already reference-covered — STM32N6 (OpenMV), nRF9151/MDBT53 (Rockbox), camera/mic (OpenMV), BQ24075 charger (both) all grade-A; only the 18650 cell needs sourcing. Created `plans/product_edge_ai_device/` (state, feasibility with the C01–C07 role map + boundary, POC tasks E0–E9) and produced the first real POC artifacts via existing bodesign capabilities: `c07-prd-seed.json` (R5: 7 subsystems incl. camera+audio, with clarifying questions), `design-evidence-manifest.json` (R6 across both corpora), and the headline `poc-readiness-guide.md` (teaches per-section + per-external-process what's required, who produces it, bodesign's contribution, and how to prepare it — grounded in Rockbox's real C04 stackup/Gerber, C06 EVT bring-up/verification, C02 STEP/assembly artifacts). No code changed this round (used R5/R6); suite remains green. Next: E4 (close clarifying questions) → E5/E6 (harvest subsystem pinout evidence → draft dual-chip core schematic via R7, kicad-cli-validated, explicitly a draft for the engineer).

- TheSmartAI package + interaction-model/scope refinements (2026-06-06): created the real client deliverable folder `gdrive/@利善美/03.研發資料/03.TheSmartAI/` mirroring Rockbox C01–C07, with a top README and a guided PRD draft (`C07-PRD/TheSmartAI_Project_requirements_draft.md`) that mirrors AcuSense's actual 12-section Project-Requirements structure (read from the Rockbox PRD PDF) — pre-filled from spec/reference evidence with explicit [NEEDS YOUR INPUT] prompts and per-section "how to fill" guidance. Three model refinements captured into `product_edge_ai_device`: (1) **interaction is prompt-driven, not a design GUI** — the web surface is for viewing/review only; (2) **C03 circuit design is a complete self-contained AI-EDA lifecycle** (requirements→schematic→netlist→ERC→BOM→pin-allocation→review, kicad-cli-validated, gap-reported), producing a factory-bound circuit design rather than a GUI draft; downstream PCB layout/Gerber/ME/FW and physical test/cert remain external with bodesign guiding their preparation; (3) **V1 scope simplified to OpenMV-derived single-corpus** — OpenMV's existing WiFi/BLE (`LBEE5KL1YN-814`/CYW43439, confirmed in the OpenMV wireless subsystem) is sufficient, so Nordic nRF9151 cellular is deferred to V2; V1 = STM32N6 + WiFi/BLE + camera + mic + flash + power/charging + battery, all reusing already-extracted OpenMV subsystems. This makes V1 the strongest candidate for the complete circuit-design lifecycle. No bodesign code changed; suite green.

- Consolidated architecture (2026-06-06): user defined the MCP's shape — **core = KiCad's circuit-design capability across the full lifecycle (schematic → layout → simulation/verification), wrapping whatever KiCad can do; surface = a docxmcp-style file/folder processor (no web UI, retired); input may be a whole project folder (like 01.ROCKBOX); interaction is prompt-driven, files land in the client folder; any non-readable engineering file ships with a readable companion (md/csv/html/docx/pdf/xlsx/png/pptx all acceptable).** Grounded on this host (KiCad 9.0.9): `pcbnew` Python API is importable (full PCB/layout/DRC/plot), `kicad-cli pcb export` covers gerber/drill/pos/step/pdf/svg/ipc2581/ipcd356, schematic via S-expr + `kicad-cli sch erc/netlist/bom`; ngspice (simulation) and freerouting (autoroute) are not installed (add when reached). Captured a consolidated "Architecture" section + a lifecycle MCP tool surface (have→build) at the top of `product_pcb_ai_viewer/tasks.md`: have = requirement planning (R5), evidence sourcing (R6), symbol gen, schematic emit+validate, footprint map (R3), gap report (R1); build = folder-ingest (docxmcp-style decompose of a KiCad project), layout via `pcbnew`, fab outputs via `kicad-cli` (+readable companions), simulation/verify. The web UI (FastAPI HTML) is retired/legacy. Note: a PRD-file restore in the TheSmartAI gdrive folder was paused by the user mid-write (C07-PRD currently empty; content safe in context); awaiting direction.

- Folder-ingest (docxmcp-style surface) validation: user chose to start the KiCad-lifecycle MCP from the ingest surface. Added `reverse-core.project_ingest` (`ingest_project_folder` + `render_index_markdown`): walks a project folder read-only, classifies every file by EDA role/format (kicad-project/schematic/pcb-layout/gerber/drill/ipc-netlist/orcad-schematic/3d-model/document/spreadsheet/firmware/…), detects C0* document sections, flags non-readable engineering files needing a readable companion, and auto-detects companions that already exist beside a file (e.g. an OrCAD `.DSN` paired with its `.png`). Verified on the real 01.ROCKBOX project: 243 files, all 7 C01–C07 sections, 30 gerbers (flagged for pygerber/KiCad auto-render companions), and the `.DSN` correctly paired with `ROCKBOX_DVT_..._IX.png`. `tests.test_project_ingest` covers a synthetic folder (companion present vs absent) + a guarded real-Rockbox test. Full suite 82 tests pass (1 skipped). This is the first stage of the lifecycle tool surface; next stages: render readable companions on demand, then layout (`pcbnew`) + fab outputs (`kicad-cli`).

- Skill ecosystem reframe + verification (2026-06-06): user pointed out the existing KiCad/EDA skills (`kicad`, `kidoc`, `emc`, `spice`, `datasheets`, `bom`/`digikey`/`lcsc`/`jlcpcb`/`pcbway`). Reframed the division of labour: **bodesign builds only the forward-GENERATION gap** (requirements→plan, symbol gen, subsystem composition, schematic emit, folder-ingest, gap/readiness — none of which the analysis/doc skills do) and **orchestrates the skills for everything else** (analysis, documentation, simulation, sourcing, fab). Verified on a real target — bodesign's own generated OpenMV subsystem `.kicad_sch`: the `kicad` schematic analyzer ingested it cleanly (6 components incl. C1–C4, rail voltages auto-inferred 1.8/3.3V, decoupling DC-DET on both rails, plus actionable findings SS-001 no-MPN and 191× NT-001 single-pin nets — confirming the generate→analyze loop). `kidoc` (`kidoc_scaffold.py --analyze`) ingested the same project, auto-ran schematic analysis, rendered the schematic SVG, and generated power-tree + EMC-severity diagrams, but its full HDD/doc-package assembly errored on a schematic-only project (needs `.kicad_pcb`, or a schematic-only path) — so the doc-package node unlocks once layout exists. Chain confirmed: `kicad` analyze (`--analysis-dir`) → `kidoc` consumes. Captured the division of labour + verification in the architecture section of `product_pcb_ai_viewer/tasks.md`. Test byproducts cleaned from the repo; no code changed.

- Plan shape + G1 companion rendering (2026-06-06): per user direction ("update plan shape, then fill the gaps"), consolidated the plan into a **bodesign-unique build queue (G1–G6)** — everything else is orchestrated via skills. Then filled **G1**: `reverse-core.companion_render` (`render_companion` + `render_all_companions`) renders non-readable engineering files to readable companions — `.kicad_sch`/`.kicad_pcb` → PDF via `kicad-cli`, Gerber → PNG via pygerber (gerber-core), OrCAD/3D/vector reported `unsupported`. This completes the ingest→readable surface and enforces the format policy (every non-readable file can get a readable companion). Verified live: bodesign's generated OpenMV schematic → 308 KB PDF, Rockbox `L1_top.art` → 165 KB PNG, `.DSN` → unsupported. `tests.test_companion_render` covers all three paths (guarded by kicad-cli/pygerber availability, in-repo fixtures). Full suite 85 tests pass (1 skipped). Remaining gaps: G2 package readiness index, G3 generalized composer (needs G6 symbols), G4 PRD/doc emitter, G5 pin/GPIO table, G6 per-part symbol harvest.

- Interaction model + G2 package readiness (2026-06-06): user asked whether to present this as a UI wizard or a plan-builder-style stepper. Decision: **neither a UI wizard (web UI is retired) nor ad-hoc Q&A — a file-stateful, plan-builder-shaped lifecycle where the readiness index is the state/compass and the prompt-driven agent is the wizard**; each turn the agent reads the index, surfaces the single next step + the input it needs, produces the artifact (file lands), updates the index; resumable because state is a file. Built **G2**: `workflow-core.package_readiness` (`assess_package_readiness` + `render_readiness_markdown`) — scans a product folder via the ingest, scores it against a POC deliverable checklist, returns per-deliverable status (present/partial/missing/external) + readiness % + the single next step; external ID/ME/FW sections marked (not gaps). Tightened matching from role-based to name-based after a false positive (the BOM file was mis-counted as the pin/GPIO deliverable). Verified on TheSmartAI: 67% — PRD + BOM present, schematic missing → next step "run the C03 circuit-design lifecycle", external = FW/ID/ME. `tests.test_package_readiness` covers synthetic (PRD/BOM present, schematic missing, no BOM↔pinmap false match, schematic-present case) + a guarded real-folder test. Full suite 88 tests pass (1 skipped). G3 (composer) is the next major gap, blocked on G6 (per-part symbol harvest).

- Trust layer / reference cross-check (G7, 2026-06-06): user raised the core trust problem — the circuit design is a black box to a non-EE owner, who can't judge reliability; the available verification anchor is the known dev-board product documents, used as a **control group**. Built `workflow-core.reference_crosscheck`: compares bodesign's generated net set against a reference product's nets → matched (confidence) / missing (reliability gap) / extra (novel, verify) + coverage % + provenance. Demonstrated on the real artifacts: bodesign's generated flash XSPI interface vs the OpenMV control group = **75% (12/16 nets), every generated net name correct (matches the shipped product), missing 4 — `XSPIM_P2_CLK_N`/`WP#`/`INT#`/`RSTO#`** — traceable to `OpenMV-N6-Schematic-Rev4.pdf p.7` (SHA-anchored, conf 0.92). This is the trust model for a non-EE: reliability is *shown by comparison to a proven product*, not asserted — agreement = faithful reuse (high confidence for V1=OpenMV), and divergences (the missing nets, incl. the CLK_N diff-pair half) are surfaced as actionable gaps; novel parts/deviations still need the analysis skills (`kicad`/`emc`/`spice`) or EE review. `tests.test_reference_crosscheck` covers synthetic (incomplete/exact/extra) + the real OpenMV flash check. Full suite 92 tests pass (1 skipped). G7 pairs with G2 (readiness) as the "is it reliable AND complete?" answer; it also gives the next concrete generation target — wire the 4 missing flash nets — measurable against the control group.

## Remaining

- Extend Rockbox reconstruction from first-pass placement/IPC component-net summaries into spatial Gerber/drill fusion: board outline, copper tracks, zones, apertures, drill hits, pad/via matching, and drill-to-copper relationships。
- Extend first-pass cross-probing from component/net/artifact evidence lookups into layer/object-level derived IR links after geometry primitives exist。
- Extend component knowledge queue beyond Rockbox placement evidence into OpenMV reference parts and real datasheet-backed records。
- Replace remaining MCP/server/web placeholders for source ingestion, knowledge normalization, Gerber export, and layout generation with persistent job-backed implementations。
- Define client↔MCP storage-share contract for local project folders: human-facing taxonomy, hidden/system workspace convention, KiCad Happy analyzer cache mapping, manifest shape, scoped read/write permissions, cache invalidation, conflict handling, source-chunk materialization, and save-back/edit semantics。
- Implement KiCad-first foundation workflow before prioritizing Gerber→design-source or datasheet/reference→design-source automation。
- Reframe the web frontend around a KiCad project workspace before expanding specialized Gerber or datasheet reconstruction views。
- Extend datasheet extraction from local text/PDF token scanning into structured pin/electrical parsing, source trust checks, and persistent storage。
- Extend the first-pass PDF-to-src adapter into real docxmcp integration with table/page layout extraction; start with client-side two-MCP orchestration before considering an internal MCP bridge.
- Define and approve an external datasheet fetching policy before any public web download path can be enabled.
- Extend the KiCad bridge from adapter-bound plan contracts into an approved plugin/submodule execution workflow after IR geometry/net/constraint coverage is sufficient。
- Extend the reference-board workflow plan from deterministic stage contracts into persistent job-backed execution once source ingestion, knowledge normalization, spatial IR, and validation adapters are ready。
- Extend generated candidate workspace from placeholder diff/evidence review into real generated design candidate storage after layout proposal and validation adapters exist。
- Keep detailed Rockbox parsing and debug/test guidance for later phases。
