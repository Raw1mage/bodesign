# apps/web

`apps/web` hosts the `/bodesign/` viewer surface for circuit/PCB views, Gerber layers, component knowledge summaries, evidence overlays, confidence, and reports.

The first milestone is a Rockbox placeholder view served by the FastAPI backend. A richer Canvas 2D viewer will replace the placeholder after `BoardDesign IR` schemas and reconstruction outputs exist.
