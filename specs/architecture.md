# Architecture

## Product Boundary

This repository will host `bodesign` as a host-agnostic MCP server plus web server. Any MCP-capable client (IDE, agent, or HTTP caller) provides files or design instructions through MCP over a Unix socket (local) or TCP port (external). The system boundary starts with source ingestion and normalization: datasheet/schematic/BOM-like evidence extraction, Gerber/drill/IPC upload, public reference-board evidence, and normalized component/design knowledge. Those normalized records feed `BoardDesign IR` generation/reconstruction, layout planning, `/bodesign/` circuit/PCB rendering, Gerber generation, deterministic validation, approval, and export. bodesign is independently operable; no host shell or gateway owns the product boundary. Factory test/debug guidance is deferred to a later phase.

## Planned Modules

- `apps/web`: `/bodesign/` web viewer for circuit/PCB view, Gerber layers, evidence overlays, confidence, component knowledge summaries, and reports.
- `services/api`: FastAPI/Python backend for project APIs, Postgres-backed design jobs, storage access, web APIs, MCP tool handlers, AI orchestration, validation, approval, Gerber generation, and export.
- `services/mcp`: Host-agnostic MCP server exposing bodesign tools to any MCP-capable IDE/agent/HTTP client. Its HTTP landing page also publishes installable skill downloads under `/skills/{name}`: the repo-local `skills/bodesign` lifecycle skill as `bodesign.tar.gz`, plus EDA companion bundles from `services/mcp/assets/skills/`.
- `packages/design-ir`: Product-owned PCB source of truth, evidence model, schema validation, and versioning. `ComponentInstance` carries optional comparator fields (`value`, `optional`, `flexible_pin_groups`). The `compare/` submodule (G7) is a deterministic reference comparator: two-stage component matching (required first; optional reference parts free), pin-neighborhood signatures, pure-Python Hungarian assignment, symmetric-passive pin normalization (`__sym__`), FlexiblePin groups, and weighted score `S = 0.4·S_comp(Dice) + 0.2·S_attr + 0.4·S_conn` (weights centralized in `ScoringConfig`; CMP_CONFIG_INVALID on bad weights). Mismatch details are CrossCheckDiffItem dicts (dimension component/pin/component_value + first_divergence) and wrap into a ValidationEvidence envelope (`tool="crosscheck"`) via `to_validation_evidence()`. Invalid input IR fails fast (CMP_IR_INVALID, no partial comparison); same input yields byte-identical output, no LLM involvement.
- `packages/component-kb`: Datasheet ingestion, part-number resolution, pinout/package normalization, interface grouping, and layout-rule knowledge. The `spice_card.py` module (knowledge/datasheet-spice-models) is a deterministic datasheet-grounded SPICE model pipeline over vault L4: `ingest_spice_extraction` validates an extraction batch row-by-row (registry leaf / evidence sha256+page / numeric value; per-row SPX_FIELD_UNKNOWN/SPX_EVIDENCE_MISSING/SPX_VALUE_INVALID rejection; `not_found` reported but never written; all writes trust=unverified), `generate_model_card` renders byte-identical `.model`/`.subckt` cards from the L4 `spice_model.{diode,ldo,passive}` namespace (typ-selection: typ→single→SPX_PARAMS_AMBIGUOUS, no averaging; provenance comment header, no timestamps; SPX_PARAMS_MISSING/SPX_CATEGORY_UNSUPPORTED fail-fast), and `materialize_model_cards` runs an ngspice DC-op smoke (pass/fail/skipped-no-simulator; fail cards excluded) then writes cards + a `manifest.json` (`source=vault-grounded`) into `<project>/spice/models/` — the `spice` skill's model-resolution cascade tier-1 cache, so vault-grounded models win without any skill change. LLM participates only in upstream extraction; this module is fully deterministic.
- `packages/doc-core`: OpenMV schematic/datasheet evidence extraction and design-intent normalization.
- `packages/reverse-core`: Rockbox evidence extraction and IR reconstruction. Current implementation parses Allegro `cds2f` placement/BOM-like exports into component instances and IPC-D-356A records into net/pad/via summaries; Gerber/drill geometry extraction, net inference beyond IPC evidence, and component clustering remain next steps.
- `packages/source-core`: `BoardDesign IR` patch validation and source export helpers.
- `packages/gerber-core`: Python-first Gerber adapter around `pygerber`, geometry normalization, manufacturing checks, output comparison, and validation.
- `packages/shared`: Shared schemas and API types.
- `tests`: Fixture-based backend, frontend, and workflow tests.

## Data Flow

1. User provides chip models, target functions, OpenMV reference documents, Rockbox component placement/BOM-like files, Gerber/artwork, drill, IPC netlist, routing/report files, and optional annotations.
2. FastAPI stores raw files and creates a Postgres project/job record.
3. Source-ingestion pipeline classifies each input as component evidence, design-intent evidence, manufacturing evidence, or reference-board evidence.
4. Component knowledge pipeline extracts part numbers, resolves/ingests datasheets, and normalizes pinout/package/power/interface/layout knowledge.
5. Document and artifact pipelines extract evidence into a shared evidence model.
6. OpenMV flow generates initial `BoardDesign IR`; Rockbox flow reconstructs `BoardDesign IR` from component placement, IPC net/pad/via summaries, Gerber/drill manifests, and confidence evidence.
7. `/bodesign/` retrieves `BoardDesign IR`, layer summaries, parsed component positions, IPC net summaries, confidence overlays, component knowledge, and generated output data for Canvas 2D rendering.
8. Validation checks verify IR schema, component knowledge coverage, connectivity evidence, DRC-like constraints, and generated Gerber outputs.
9. AI invokes EDA MCP tools to ingest knowledge, propose layout plans, reconstruct Rockbox, generate datasheet-derived designs, or create IR operations; deterministic validators gate every operation.
10. User reviews generated layout/Gerber candidates in `/bodesign/` or an IDE/host proposal UI and approves selected outputs.

## External Technology Baseline

- OpenMV schematic/datasheet extraction: needed for datasheet-to-layout flow.
- `ComponentKnowledge`: normalized datasheet and pinout knowledge is a Day 1 dependency for layout generation/reconstruction quality.
- `BoardDesign IR`: core product-owned EDA kernel and source of truth.
- Any MCP-capable client (Cursor, Claude Desktop, VS Code, custom agents/HTTP callers) can drive bodesign through MCP; no single host owns the product boundary.
- KiCad bridge: first practical target for layout file/export/DRC compatibility, kept behind adapter boundaries.
- `freerouting/freerouting`: candidate deterministic autorouter after IR emits placement, nets, keepouts, and routing constraints.
- `skidl`: optional OpenMV document-to-circuit/netlist evidence layer.
- `Argmaster/pygerber`: primary Gerber/artwork inspection/render candidate for FastAPI integration.
- IPC-356 parser: needed for net connectivity evidence when available.
- Drill parser: needed for vias, through-holes, and drill-to-copper checks.
- `tracespace/tracespace`: web visualization and layer-identification reference; not core dependency unless maintenance risk is accepted.
- `gerbv/gerbv`: mature comparison/reference viewer; GPL/native concerns prevent default embedding.

## Safety and State Rules

- AI suggestions are advisory until converted into typed `BoardDesign IR` operations.
- No layout or Gerber output is considered final without user approval.
- AI does not directly route copper or emit final fabrication files; deterministic IR validation, bridge export, and Gerber validation are required.
- Patch validation is deterministic and must fail fast on unsupported operations.
- `BoardDesign IR` is the source of truth for generated/reconstructed layouts.
- Component knowledge records are evidence-linked and confidence-scored; unknown datasheets must produce explicit gaps instead of silent assumptions.
- Raw uploads, normalized evidence, `BoardDesign IR`, generated Gerbers, findings, proposals, approvals, and exports remain separate states.

## Verification Baseline

- Component knowledge extraction, document extraction, artifact parsing, IR generation, and reconstruction behavior should be fixture-driven.
- First reconstruction spike now verifies placement and IPC inputs can recover component instances, copper layer labels, named nets, pad counts, via counts, and confidence evidence. Next reconstruction spikes must add Gerber/drill geometry for board outline, tracks, zones, apertures, and drill-to-copper relationships.
- Gerber validation spike must verify `pygerber` support for layer bounds, apertures, flashes, tracks, regions, SVG/image rendering, and output comparison.
- API flows should verify upload, parse status, findings, proposal creation, approval, and export.
- Frontend tests should cover viewer controls and approval UI state transitions.

## Immediate Capability Gaps

- Project workspace and document management: current web UI has a builtin Rockbox fixture project, but real client-shared project folder browsing/editing is still missing. The intended storage boundary is client-owned local folders: users manage all raw documents, extracted source chunks, datasheets, schematics, board files, edits, and generated artifacts in project folder structure on the client side. The MCP server is a lightweight analyzer/indexer/web surface that may cache render outputs and parse indexes, but cache is disposable and not authoritative.
- File visualization: source files must be rendered in type-specific viewers before the product claims a board/circuit view. PDFs, BOM/placement, IPC nets, Gerber metadata, drill files, and routing reports each need their own visual surface.
- Board View: must stabilize on a third-party raster/CAD-style Gerber renderer before further reconstruction work. The current work-in-progress is moving from pygerber SVG/fallback experiments to pygerber raster output with explicit fail-fast errors when rendering is unavailable. Decorative placement sketches and hand-written SVG approximations are explicitly disallowed as the default PCB view; pan/zoom/layer toggles, multi-layer compositing, and full board-outline-aware rendering remain pending.
- Geometry reconstruction: gerber-core now parses RS-274X aperture/flash/draw/region/polarity summaries, normalizes Allegro compatibility header blocks for pygerber, and parses drill tool/hit summaries for Rockbox fixture files. design-ir now defines a first `GeometryPrimitive` type, and reverse-core emits net-bearing via primitives from drill↔IPC fusion. Full board outline detection, repeated drill command expansion, polarity-aware regions, and copper-track/pad/region `GeometryPrimitive` coverage remain pending.
- Connectivity reconstruction: the current first-pass fusion summarizes placement components against IPC pin/net evidence in the geometry API and Board View. reverse-core `fusion.py` adds spatial drill↔IPC-via fusion: drill hits and IPC 317/327 features share a coordinate frame (drill `raw/1000`, IPC `raw/100`), so each plated drill hit is matched to its nearest via and assigned that net, with unmatched holes classified as tooling/mounting (Rockbox: 783/789 matched, 6 unmatched, 92 via nets). The geometry API exposes this as a `spatial_fusion` envelope. The Allegro `cds2f` placement export uses a different origin and is deliberately left uncoregistered (no faked agreement) until a board-origin transform is solved. First-pass cross-probing links component, IPC net, and source artifact evidence through `/cross-probe/{probe_id}`. Spatial fusion of Gerber pads/flashes, placement↔IPC co-registration, confidence scoring, and layer/object-level derived IR cross-probing remain pending.
- Knowledge base: component-kb now has a **persistent server-side Component Vault** (`storage.py` + `repository.py`): SQLite (WAL, foreign-keys, `user_version` migrations v1–v5) + FTS5 + content-addressed blob store under `BODESIGN_VAULT_DIR` (docker named volume `bodesign-vault`; missing env/dir or corrupt DB fail fast — VAULT-E001/E002, never rebuilt silently). Eight layers: L1 identity (canonical MPN + alias resolve, explicit absent), L2 documents (sha256 dedup, version chains, mandatory provenance), L3 chunks (doc-core `DocumentSourceChunk` adapter, FTS5 BM25 search with page anchors, extractor upgrades mark old chunks stale instead of deleting), L4 specs (EAV with field_path registry seeded from `FIELD_ALIASES`, min/typ/max + condition coexistence, `trust='verified'` requires evidence at the trigger level; `resolve_field_path` does longest-prefix root matching so multi-segment roots like `spice_model.diode` resolve alongside single-segment ones; the `spice_model.{diode,ldo,passive}` namespace — a closed v1 `SPICE_MODEL_FIELDS` registry — holds datasheet-grounded SPICE parameters consumed by `spice_card.py` to generate cascade tier-1 model cards), L5 EDA assets (symbol/footprint mappings with a one-rung verification ladder unverified→pin-checked→drc-passed, provenance per upgrade; `kicad_emit.vault_symbol` / `footprint_map.vault_footprint` consume via duck-typed repository and return explicit absent), L6 app knowledge (layout-rule / reference-circuit / companion-part / design-rule payloads, evidence-gated trust), L7 trust/audit (append-only `audit_log` enforced by triggers; every repository write is one audited transaction), L8 usage/sourcing (`record_usage` per project, cross-project occurrence aggregation feeding `knowledge_queue`/`component_completeness` views, point-in-time sourcing snapshots + substitutions). Client caches import through `import_client_cache` (always unverified, `client-cache-import` provenance, conflicts keep both sides and report VAULT-E903). API surface: 4 MCP tools (`bodesign_vault_ingest/query/spec_check/queue`, core group) + 5 HTTP routes share the thin `services/mcp/vault_api.py` layer; `vault.py spec_check` consults the server vault first and marks each verdict's origin (`server-vault` | `client-cache`) while preserving four-state semantics. The legacy in-memory `/knowledge/queue` remains as the placement-evidence entry point. Fixture suites: `tests/test_vault_{storage,chunks,specs,api,usage,eda}.py`. Remaining: OpenMV reference extraction and deeper emit-flow integration of vault-backed EDA mappings.
- EDA bridge: KiCad is introduced behind adapter boundaries through `eda-bridge`. Beyond the plan contract, `eda-bridge/kicad_emit.py` now provides a **real** IR→KiCad emitter: it loads symbol definitions/pin endpoints from the installed KiCad symbol libraries, embeds them into a self-contained `.kicad_sch`, connects pins via global labels at pin endpoints (handling the library Y-flip), writes `.kicad_pro`, and validates with `kicad-cli sch erc` + `sch export netlist` (fail-soft when `kicad-cli` is absent). The emitter also supports a drawn-wire connectivity mode (`connection_style="wire"`: 2-pin orthogonal L-route, 3+ pin channel route + junctions, all grid-snapped to KiCad's 1.27 mm connection grid). On top of it, `compose_schematic` gained an opt-in `style="draftsman"` (BR `issue_20260618`, plan `plans/eda_schematic-draftsman-quality/`) that produces engineer-readable schematics — subsystem clustering (declared `group` or net-degree, hub degree-capping) → **deterministic** force-directed refinement (fixed init/iters, no RNG) → AABB overlap re-spacing → drawn wires → smallest-standard-sheet fit + centering; power/bus/single-pin/unresolved nets fall back to global labels with visible `route_stats.label_fallback_reasons`. **`style="netlist"` stays the default and is byte-equivalent to the pre-upgrade naive-grid+label path (no silent behavior change)**; explicit `connection_style` is honored over the style default. A toolchain-gated `ink_metrics.py` (kicad-cli PDF→pdftoppm→PIL) measures ink%/content-fill for regression, reporting `available=false` + missing tools rather than fabricating a number. KiCad 9.0.9 is installed on this host for end-to-end validation. `kicad-cli` is used as an external subprocess over the open `.kicad_sch` format — using the tool, not linking GPL source. Forward-design path: AI does not synthesize netlists from scratch; it starts from target chips' dev-board datasheets/reference designs, composes subsystems into IR, and emits through this bridge. C04 MCP coverage now includes netlist-to-PCB, via-in-pad fanout, plane pour, DRC gate, SI check, autoroute, pure-python impedance estimates, clearance-safe bus widening, clearance-aware bus length matching, and real Gerber single-layer preview through `gerber-core`/pygerber. `pcbnew`/KiCad-mutating tools route to the EE worker; pure impedance and Gerber preview remain core and fail explicitly on unsupported inputs or unavailable renderers. EDA-bridge generality contract (no SILENT overfit): board/process assumptions are caller-overridable or reported, never silently applied to a non-reference board — `route_net2pcb` connector pin expansion takes an explicit `connectors` map and otherwise applies the built-in USB-C table to any USB-C footprint on any refdes (not just J1), reporting `applied_pinmaps`/`unmapped_connectors`; `si_check` exposes `rdrv`/`cload`/`edge_ns`/overshoot thresholds (documented STM32-class CMOS defaults) and echoes the `effective` values. All previously-baked process defaults are now caller-overridable too — `emit_layout` (board_mm/columns/place_start_mm/place_pitch_mm/margin_mm), `emit_fab` (pdf_layers), `via_in_pad` (drill_mm/pad_mm/keep_rings), `pour_planes` (stitch_net/stitch_pitch_mm/stitch_drill_mm/stitch_pad_mm) — with documented small-board/JLCPCB-class defaults. The contract is regression-guarded by `tests/test_tool_generality.py` against the 5-axis checklist in `docs/generality-check.md`. Flagship-new parts absent from KiCad's standard libraries (e.g. STM32N6) require datasheet-pinout-driven symbol/footprint generation. Current emitter limits: single-unit symbols and rotation-0 placement; multi-unit symbols, rotation, generalized footprint/PCB emission, safe multilayer Gerber compositing, and fabrication approval remain pending.
- AI design workflow: workflow-core exposes a deterministic reference-board workflow plan for ingest sources → resolve component knowledge → reconstruct/reference-board IR → propose subsystem/layout intent → **design-review** → deterministic validation → user approval. Stage status is now **derived from the orchestration spine** (`derive_workflow_plan(folder)`: `_orchestration/` work packets + blockers + evidence returns are the single source of truth; `plan_reference_board_workflow()` is demoted to a pure stage-template generator). A missing `_orchestration/` directory reports explicit `SPINE_NOT_INITIALIZED` blockers — never a silent fallback to parameter-snapshot status. The design-review gate (G2) validates a persisted `DesignReviewRecord` (`_design_review/design_review.json`: subject, scenario walkthroughs with severities, APPROVE/APPROVE_WITH_CONCERNS/REJECT verdict); a missing record (`REVIEW_MISSING`) or `REJECT` (`REVIEW_REJECTED`) keeps deterministic-validation blocked. Verification discipline contracts: `ExtractedRequirement` is contractualizable (`metric`/`threshold`/`measurement_method`/`oracle_tool` closed enum/`verification_status`; `oracle_tool="none"` forces `unverifiable` + open-question escalation; `requirement_passfail_table()` never infers pass without an oracle execution record), `BlockerReturn.simple_fix_candidates[]` gates structural proposals until every cheap hypothesis is ruled out with evidence, `ValidationEvidence` envelopes (`validation_evidence.py`: drc_gate/si_check/crosscheck/spice adapters, raw_result preserved untouched; the `spice` adapter maps simulate warn/fail subcircuits — surfacing each finding's `model_source` so a generic-default model behind a result stays visible — and failed model-card smoke into findings) flow back to C00 as the spine's third artifact class `evidence_returns/` (`bodesign.c00.evidence_return.v1`, count-based `<LAYER>-EV-NNNN` IDs, malformed payloads fail fast and persist nothing; `ingest_evidence` records per-requirement verdicts and never auto-executes fixes). `crosscheck_diff()` generalizes net crosscheck into a multi-dimension `CrossCheckDiff` (net/pad/component/pin/component_value/layout_rule items with severity + `first_divergence`; missing-evidence dimensions reported as `dimensions_unavailable`, never faked as matched) and `record_root_cause()` persists four-part root-cause reports (methodology/findings/anchored evidence/fix) to `_root_cause/` + spine log. MCP surface adds `bodesign_reference_board_workflow`, `bodesign_wrap_validation_evidence`, `bodesign_return_evidence`, `bodesign_list_evidence_returns`, `bodesign_ingest_evidence`. The legacy `/candidates/generated-design` workspace is unchanged; the Candidates web tab shows diff/evidence/validation review with approval state defaulting to `not-approved`. Generated layouts remain non-final until export validation passes.

- C01 industrial design / ID-native deliverable buckets: beyond the five Rockbox-like companion artifacts (`c01_emit_package` → `Ai file/Design_Direction.md`, `CMF/CMF_Direction.md`, `Display UIUX/UIUX_Requirements.md`, `Interface_Constraints.json`, `Handoff_to_ID_Designer.md`), workflow-core `c01_id_package.py` now emits three **from-zero ID-native deliverable buckets** (the BR `issue_20260617_c01_rockbox_style_id_deliverables`, plan `plans/c01_design-vector/`): `emit_c01_id_visual_package` → `C01-ID/Ai file/<product>_ID_skeleton.svg` (deterministic glyph-library S2 assembly: `outline/panel/cmf-fill/components/annotations` layers, each exposed component a named `component-<type>-<n>` group; uncovered components become labelled placeholders, never silently dropped) + `<product>_ID_skeleton.preview.html` (DD-9: browser-viewable, zero-dependency preview that embeds the SVG inline with a draft banner + layer/placeholder legend — always emitted) + `<product>_ID_skeleton.png` (DD-9: static raster `.ai` stand-in via cairosvg, toolchain-gated — `png_rendered=false` + **not listed in `files`** when cairosvg is absent, no phantom deliverable) + `figma_import_spec.json` + `README.md`. The three honest `.ai` substitutes are the editable SVG source (the real substitute), the HTML preview (review without a design tool), and the optional PNG raster (slides/attachments) — the Illustrator `.ai` itself is never fabricated; `emit_c01_cmf_package` → `C01-ID/CMF/cmf_tokens.json` (material family / finish / colour routes / RF-transparent zones / gasket / sample-vendor gates, `approval_state` permanently `not-approved`) + a `_CMF_Direction` markdown→PDF + `README.md`; `emit_c01_uiux_package` → `C01-ID/Display UI_UX/uiux_wireframes.svg` + `_UIUX_Flow` markdown→PDF + `README.md` (OLED/LED/insert-remove/privacy/charging/error states; a no-display product maps **explicitly** to LED/status/button interaction, not silent omission). MCP tools `bodesign_c01_emit_id_visual_package` / `_emit_cmf_package` / `_emit_uiux_package`. Honesty contracts (the BR's core ask): each emitter fails fast (`missing`/`external-needed` + the missing field list) when its key answer_state field is absent — no default form/material/state silently substituted; `.ai` is **never** fabricated (SVG + `figma_import_spec.json` stand in until a real Illustrator export path exists, `ai_emitted=false`); every visual/document carries a visible draft marking (`not final industrial design` / `not CMF approval` / `not UI sign-off`) and `draft_markings` is never empty; PDFs render through the approved `emit_document` (LibreOffice) pipeline — when the toolchain is absent the markdown source is the truth and the `.pdf` is reported `pending` and **NOT listed in `files`** (no phantom deliverable). `assess_c01_package_readiness` (`bodesign_c01_readiness`) is dual-track: the legacy `readiness_pct`/`usable`/`artifacts` keep their core-companion semantics (backward compatible) and gain `companion_readiness` (five source-of-truth artifacts) + `id_native_readiness` (the three optional/demo buckets); ID-native outputs never flip `human_approved` to true or promote a package draft→approved. NOTE the relationship to C02's 2D path: the C02 `c02_project_svg` projection (a 2D *drawing of the enclosure geometry*, derived from the 3D STL, single-source-no-drift) supersedes the original "vectorise the product silhouette" reading of `c01_design-vector`; the C01 buckets here are a **different deliverable class** — description-driven ID hand-off scaffolds (visual skeleton + CMF + UI/UX), authored from `Interface_Constraints.json` + answer_state, not a metric drawing of the case.
- C02 mechanical / voice-to-design: the enclosure path (workflow-core `c02_me_package.py`) generates a parametric OpenSCAD solid from **explicit** constraints (board outline, component heights, mounting holes, connector openings, wall/clearance/lid) and never guesses dimensions — missing required geometry fails fast. Both the OpenSCAD path (`_render_openscad_source`) and the build123d/STEP path (`_build_enclosure_part`) consume the SAME shared geometry helpers `_normalize_face` (east/west/north/south + CN/EN aliases, illegal→None) and `_connector_cut_geometry` so they do not drift: connector openings become **real through-wall cuts** (`connector_cuts()` difference on the named face — east=+X/west=-X/north=+Y/south=-Y wall, `z_mm` = hole-centre height above floor top z=wall, optional `offset_mm` else centred) and mounting holes become **real standoff bosses + pilot holes** (`mounting_posts()`, dia+3 boss / dia through pilot), not comment-only notes or marker bumps. A connector opening needs all four of {face, width_mm, height_mm, z_mm} to be cut; incomplete geometry is skipped and marked pending (DD-2 never guesses). Optional vertical-edge rounding: an explicit `corner_radius_mm` rounds the 4 standing edges on both paths (OpenSCAD `_outer_shell_scad` swaps the outer cube for a `hull()` of 4 inset `cylinder`s; build123d `fillet`s the `Axis.Z` edges) — `None`/0 keeps square corners (backward-compatible, not a guess) and a radius > min(case_w,case_h)/2 fails fast instead of silently clamping; the voice front end extracts it from 圓角/倒角/fillet/chamfer keywords and asks for the radius (DD-2) when the feature is mentioned without a number. `c02_export_stl` (OpenSCAD CLI) and `c02_export_step` (build123d/OCP) export real meshes/solids, toolchain-gated; `c02_export_skp` stays honestly unavailable (import guide only). Enclosure rendering reuses the board render backend: `eda-bridge/model_render.py` factors the pyrender/EGL stage into a shared `_render_mesh_views`, so `render_enclosure_model` loads an STL via trimesh and renders top/iso PNGs through the same offscreen pipeline as `render_board_model` (degrades to no-deps/no-gl rather than crashing). CMF single-colour: `render_enclosure_model` takes an optional `color` (hex `#RRGGBB[AA]` / EN+中文 colour names / RGBA tuple) resolved by `_resolve_cmf_color`; `None` keeps the neutral grey (backward-compatible), bad input falls back to grey with a note. 2D vector design view is **derived from the same 3D model** (no separate LLM-authored 2D path): `export_c02_projection_svg` (MCP `c02_project_svg`) writes a `projection(cut=<bool>) import(Enclosure.stl)` wrapper and runs `openscad -o Enclosure.svg`, yielding exact geometric vector paths (incl. corner arcs) — not traced raster fragments — and fails fast (source_missing / export_unavailable) rather than faking an SVG. This supersedes the standalone `c01_design-vector` plan (2D now flows from 3D, single source, no drift). The voice front end extracts `cmf_color` from spoken colour words and `voice_to_design` threads it into render; the projection SVG is emitted into the `voice_to_design` pipeline under a `projection` key. The **voice-to-design** front end (`plan_c02_intent` + `voice_to_design`, MCP tools `bodesign_c02_plan_intent` / `bodesign_c02_voice_to_design`) does deterministic keyword+regex extraction of the 8 C02 constraint fields from a spoken spec (mirroring `plan_design_intent`'s three-state answered/stated/missing pattern), surfaces a clarifying `next_question` for missing dimensions instead of guessing, and honours an approval gate — only with `approve=true` AND complete constraints does it run generate_openscad → export_stl → render_enclosure. `c02_voice_to_design` routes to the ME worker; `c02_plan_intent` is pure-python core.

Doc-source pipeline: doc-core now includes a first-pass `DocumentSourceChunk` adapter that decomposes local text/PDF-like documents into chunked source assets with `EvidenceRef` anchors before downstream extraction. This is the local contract for the proposed docxmcp-style PDF workflow. The preferred MVP integration is client-side orchestration across bodesign MCP and docxmcp; an internal MCP bridge is possible later only behind an adapter boundary so bodesign does not depend directly on docxmcp runtime/accounts/permissions.

C00 PRD docx emission (the first concrete landing of the bodesign↔docxmcp orchestration): `workflow-core/c00_prd_docx.py` (`render_c00_prd_docx_package`) renders the C00 PRD as a docxmcp-assemblable package — `body.md` + `outline.md` + `manifest.json` + `template/template.dotx` per document — reproducing the real Rockbox C07-PRD Word architecture (cover block + Revision History table + 12 numbered Heading-1 sections + in-section table schemas). The Word architecture is stored server-side as a `.dotx` binary + a `docx_architecture.json` descriptor (`workflow-core/.../templates/`: `c00_prd.dotx`, `c00_rf.dotx`, `c00_prd.docx_architecture.json`). The MCP tool `bodesign_c00_emit_prd_docx` (handler `_h_c00_emit_prd_docx`) renders the package(s) then calls docxmcp `document.assemble` via `mcp_delegate.call_external_mcp_tool` (`render_only` short-circuits before assemble). Honest field-state discipline holds: missing/drafted field states stay visible in the rendered body and the renderer never marks human approval. docxmcp gotcha: `assemble` needs an ABSOLUTE container `doc_dir` (`/var/cache/docxmcp/sessions/<token>/…`); a relative path resolves against `/app` and fails.

Client↔MCP storage share: the product should follow a docxmcp-like split where the client owns durable files and exposes a scoped project folder manifest/read/write capability to the MCP. The MCP can analyze, split PDFs into source chunks, render online views, suggest edits, and write back through that scoped share; it should not silently migrate user content into server-owned durable storage. Required open design points include manifest schema, permission scope, cache invalidation, conflict handling, and whether writes are direct MCP writes or client-applied patch operations.

Project folder UX: users should not see an exploded machine intermediate tree at the root. The human-facing project root should stay close to KiCad/EDA concepts: `docs/` or `inputs/` for datasheets, application notes, reference designs, and incoming manufacturing packages; `eda/` for KiCad-native `.kicad_pro`, `.kicad_sch`, `.kicad_pcb`, project settings, and reviewable schematic/board sources; `libraries/` for project-local symbols, footprints, 3D models, and vendor libraries; `outputs/` for reviewed PDFs, Gerbers, drill files, BOM, pick-and-place/position files, STEP/3D exports, and release packages; `reports/` for reconstruction/validation summaries. Machine-only data — source chunks, OCR/table extraction, normalized IR, parse caches, embeddings, render artifacts, provenance indexes, workflow state, and temporary bridge outputs — belongs in a hidden/system workspace such as `.bodesign/`. The MCP manifest maps standard EDA folder locations to semantic roles and the MCP web UI exposes internals as evidence views without making the underlying folder structure noisy.

KiCad Happy integration: the existing KiCad Happy skill uses `.kicad-happy.json` and an analyzer cache convention (`analysis/<run_id>/`, `analysis/manifest.json`, optional report figures). bodesign should interoperate with that convention but default MCP-run analysis to hidden cache paths such as `.bodesign/analysis/kicad-happy/` with `track_in_git=false`, then present findings, trust summaries, diffs, renders, DRC/ERC/DFM/EMC/thermal evidence, and report inputs through the web UI. Visible `analysis/` folders are an opt-in compatibility mode for engineers already using KiCad Happy directly.

KiCad-native foundation gate: near-term product work should focus on making client-owned KiCad projects usable through native KiCad plus bodesign companion surfaces before trying to complete Gerber→design-source or datasheet/reference→design-source automation. Native KiCad remains authoritative for schematic/PCB editing, canvas behavior, libraries, and DRC/ERC. bodesign supplies storage-share, file/library/output detection, hidden KiCad Happy analysis cache, Web evidence views, reports/manufacturing output browsing, AI workflow state, candidate review, and safe save-back/patch semantics. The two conversion paths remain research-grade until native KiCad plugin/sidecar workflows can reliably browse, analyze, propose, and round-trip ordinary KiCad projects.

KiCad plugin scaffold: `packages/kicad-plugin` is the first native entrypoint. It is intentionally importable without KiCad for tests, degrades when `pcbnew` is unavailable, and only represents sidecar operations: active project discovery, bodesign dashboard URL generation, sidecar handshake payloads, analysis request construction, and approved-patch request representation. The bodesign API exposes a deterministic plugin handshake that reports sidecar readiness, URLs, approved capabilities, blockers, and approved-patch-only policy. It must not run DRC/ERC or mutate `.kicad_*` files until a user-approved KiCad-native plugin workflow exists.

Client folder browsing: the next storage surface should expose a manifest-derived project tree, not raw server filesystem access. It should present human-facing KiCad/EDA folders (`docs`/`inputs`, `eda`, `libraries`, `outputs`, `reports`) and summarized hidden `.bodesign` cache/evidence state in the companion dashboard. Read/write scope is governed by storage-share policy; initial implementation remains read-only/fixture-backed until client-provided folder handles and approved save-back semantics exist.

Save-back proposals: edits generated by bodesign should first be represented as evidence-backed client-applied proposals, not direct writes. A proposal records target path/scope, operation intent, evidence refs, approval state, conflict policy, and application instructions for the client or native KiCad plugin. Direct MCP-side mutation of client files is blocked unless a later explicit save-back workflow is approved.

Cache/conflict status: MCP cache is disposable evidence and must be invalidated or refreshed from client-owned source anchors. Conflict handling should be explicit: report source revision anchors, stale cache markers, proposal conflicts, and required client refresh/approval actions. The MCP must not silently resolve conflicts or treat cache as authoritative project storage.

Source-chunk materialization: docxmcp-style PDF/text decomposition should be represented as provenance-preserving chunk plans targeting hidden `.bodesign/sources/` paths under client control. The MCP may propose chunk manifests, evidence anchors, and cache/materialization status, but client-side orchestration owns durable source chunks and grants/copies them explicitly.

KiCad analysis request/status: native KiCad/KiCad Happy analysis should be represented as an approval-gated request/status contract before any execution workflow exists. The contract should list requested checks, native plugin/client orchestration mode, hidden `.bodesign/analysis/kicad-happy/` target cache paths, expected evidence outputs, current run state, and blockers. The bodesign server must not invoke KiCad, run DRC/ERC/analyzer subprocesses, mutate `.kicad_*` files, or treat analysis cache as authoritative source data.

KiCad analysis evidence manifest: after client/native plugin execution, bodesign should expose a manifest-style index over hidden KiCad Happy evidence outputs rather than raw filesystem browsing. The manifest records produced artifact refs, trust summaries, diffs, renders, reports, DRC/ERC/DFM/EMC/thermal evidence, freshness, and blockers. It remains disposable cache metadata; durable sources stay in the client-owned KiCad project folder.

Project registry: bodesign should route projects through lightweight client-owned project records rather than a hard-coded demo id or server-owned durable store. A registry record names the project, stores non-authoritative folder-handle metadata/status, links storage-share/project-tree/KiCad-native surfaces, and reports blockers for real client handles and save-back. The registry itself is metadata/evidence; durable source files remain under client control.

Folder open/import request: bodesign represents client-side folder handle requests but does not open arbitrary server paths. The request contract records requested permissions, read/write scopes, `needs-client-grant/not-approved` approval state, fail-fast blockers, and post-grant refresh actions for registry, storage-share, project-tree, and KiCad foundation surfaces. File reads/writes remain unavailable until the client grants a scoped handle and approves save-back/conflict policy.

Frontend axis: the web frontend is a KiCad companion dashboard, not a KiCad-native editor. Top-level experience should organize around Project Overview, Schematic Status, PCB Layout Status, Libraries, Datasheets/Docs, Analysis, Manufacturing Outputs, Reports, and Candidate Review. Gerber/drill/IPC/BOM/placement/raster/reconstruction data remain important evidence panels and cross-probe surfaces. Actual schematic/PCB editing, KiCad canvas operations, and DRC/ERC execution belong in native KiCad via plugin/sidecar integration.

External datasheet policy: automatic public web downloads are disabled by default. `/knowledge/external-fetch` is a fail-fast policy gate that returns an approval-required response; accepted inputs before approval are user-provided PDFs/text and docxmcp-derived source chunks.

Vault diagnostics: `bodesign_vault_diagnostics` and `GET /vault/diagnostics` are the supported inspection path for live server-vault state. They run inside the same `BODESIGN_VAULT_DIR` storage boundary as normal vault tools, report DB presence/size plus a queue preview, and must not fall back to a temporary vault or require direct host access to Docker volume internals.
